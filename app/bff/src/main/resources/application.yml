spring:
  application:
    name: bff
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${PENNYWISE_SECURITY_OIDC_ISSUER_URI:}
  graphql:
    path: /graphql
    schema:
      locations: classpath:graphql/
    websocket:
      path: /graphql

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      service: bff

pennywise:
  security:
    oidc:
      audience: ${PENNYWISE_SECURITY_OIDC_AUDIENCE:}
