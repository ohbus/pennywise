meta {
  name: Expired token is rejected by Notifications
  type: http
  seq: 17
}

get {
  url: {{baseUrlNotifications}}/notifications/v1/preferences
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{expiredToken}}
}

tests {
  test("expired token is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
