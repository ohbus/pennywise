# PR-30 — Subscription revocation on membership removal

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-20, CORE-01
- Owned paths: BFF realtime fanout/event consumer, focused tests, and this task detail

## Objective

Ensure a member removed from a group cannot continue receiving GraphQL
subscription updates until the subscription TTL expires.

## Acceptance criteria

- `member.removed` events revoke the removed subject's active subscriptions for
  the affected group before subsequent updates are published.
- Other members' subscriptions remain active.
- Duplicate removal events remain idempotent through existing event
  deduplication.
- Focused tests prove removed members receive no later group updates.

## Validation

- `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

`member.removed` event processing now revokes the removed subject's
subscriptions for that group before publishing the update. A focused consumer
test proves the removed subscription receives nothing while another member's
subscription remains active.
