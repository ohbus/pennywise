# QA-07 — Exhaustive public-interface integration coverage

## Objective

Create and execute a contract-driven coverage matrix for every implemented REST
operation, GraphQL query/mutation/subscription, and WebSocket lifecycle. The
matrix must distinguish unit, adapter/controller, persistence, live integration,
and end-to-end evidence and must not treat syntactic contract validation as
proof of semantic route coverage.

## Dependencies

- `DOC-18`
- `ERR-10`
- `QA-05`
- `DOC-26`
- `OPS-20`

## Owned paths

- `contracts/rest/`
- `contracts/graphql/`
- `tests/acceptance/`
- `tests/e2e/`
- `tools/contracts/`
- `tools/errors/validate_catalog.py`
- `tools/ops/check_security_hygiene.py`
- `tools/ops/validate_release_gate.py`
- `app/bff/src/test/kotlin/com/subhrodip/pennywise/bff/BffFanoutTest.kt`
- `app/bff/src/test/kotlin/com/subhrodip/pennywise/bff/GraphqlHttpTransportTest.kt`
- `app/accounts/src/test/kotlin/com/subhrodip/pennywise/accounts/ProfileControllerTest.kt`
- `app/expense-core/src/test/kotlin/com/subhrodip/pennywise/expensecore/groups/GroupControllerTest.kt`
- `app/expense-core/src/test/kotlin/com/subhrodip/pennywise/expensecore/recurring/RecurringExpenseControllerTest.kt`
- `app/expense-core/src/test/kotlin/com/subhrodip/pennywise/expensecore/settlements/SettlementControllerTest.kt`
- `app/expense-core/src/test/kotlin/com/subhrodip/pennywise/expensecore/settlements/JpaSettlementStoreTest.kt`
- `app/expense-core/src/main/kotlin/com/subhrodip/pennywise/expensecore/settlements/InMemorySettlementStore.kt`
- `app/expense-core/src/main/kotlin/com/subhrodip/pennywise/expensecore/settlements/JpaSettlementStore.kt`
- `README.md`
- `docs/implementation/technology-decisions.md`
- `docs/quality/`
- `docs/api/implementation-status.md`
- `docs/tasks/details/QA-07.md`
- `docs/tasks/progress.md`

## Acceptance criteria

1. Every implemented public operation has a matrix row linked to its contract,
   implementation, and executable test evidence.
2. Each REST and GraphQL operation covers applicable success, validation,
   authentication, authorization, not-found, conflict, idempotency,
   pagination/boundary, malformed-input, timeout, dependency-failure, and
   persistence/side-effect cases.
3. GraphQL HTTP, WebSocket handshake, subscription filtering, reconnect/error,
   authorization, queue, expiry, and broker-recovery behavior are exercised.
4. Concurrent updates, stale revisions, duplicate replay, outbox delivery,
   notification deduplication, and recovery are verified through public
   interfaces where the behavior is externally observable.
5. OpenAPI path structure is semantically validated and agrees with controller
   routes; the QA-05 deterministic harness no longer disagrees with its live
   rollback classification.
6. Quality, API, operations, task tracker, and progress documentation records
   exact commands, evidence paths, runtime limitations, and uncovered cases.

## Validation commands

- `python3 tools/contracts/validate.py`
- `python3 tools/contracts/validate_public_surface.py`
- `./gradlew test --rerun-tasks --no-daemon`
- `make check`
- `make acceptance-live`
- `make bruno-run BRUNO_ENV=local`
- `python3 tests/e2e/test_product_journey.py`
- `python3 tests/e2e/test_offline_resilience.py`
- `python3 tests/e2e/test_concurrency_subscriptions.py`
- `python3 tests/e2e/test_chaos_recovery.py`
- `make e2e-rest-edge`
- `git diff --check`

The hosted reusable CI workflow now runs the semantic public-surface validator,
acceptance unit suite, workflow YAML parsing, complete Gradle checks, and the
live local-stack acceptance/Bruno/product/offline/concurrency/chaos/REST-edge
suite when its E2E input is enabled. The E2E job always tears down its stack.

## Known limitations

Production restore, security scanning, production-scale capacity, and
deployment rollback remain environment-dependent and must not be represented
as locally proven by this task.

## Increment evidence

- `362439c` registered the task, coverage matrix, semantic REST public-surface
  validator, and corrected the Expense Core OpenAPI path-item structure.
- The QA-05 deterministic acceptance harness now uses a threaded mock server so
  its concurrent rename scenario exercises two simultaneous requests instead of
  serializing one request until it times out.
- `python3 -m unittest discover -s tests/acceptance` passed all 10 tests.
- `make contracts` passed, including `validate_public_surface.py`, which found
  41 REST operations, 9 GraphQL root operations, and a complete Bruno request
  inventory across the three OpenAPI documents and GraphQL schema.
- `./gradlew :app:bff:test --tests 'com.subhrodip.pennywise.bff.GraphqlHttpTransportTest' --no-daemon`
  passed seven transport tests covering profile, group, settlement-suggestion,
  create-group, repayment, malformed mutation input, invalid fields, and
  upstream failure envelopes.
- Reconciled the implemented allocation-preview controller into the Expense
  Core OpenAPI path inventory and Bruno collection, including exact-total
  assertions for an uneven split.
- `make bruno-run BRUNO_ENV=local BRUNO_TOKEN=test-user` passed 44 requests
  against the local stack, including the new allocation-preview request and
  the full Accounts, BFF, Expense Core, recurrence, settlement, sync,
  lifecycle, and quality collections. The expected mark-as-read request was
  exercised without a seeded notification and returned its documented negative
  response; a seeded positive read assertion remains part of the live
  notification journey.
- `python3 tests/e2e/test_concurrency_subscriptions.py` passed against the live
  Docker stack: authenticated RFC 6455/`graphql-transport-ws` handshake,
  `groupChanged` delivery, concurrent stale-version conflict, and stale-client
  recovery all succeeded.
- `make e2e-rest-edge` passed 12 live REST checks covering missing
  authentication, malformed identifiers, allocation validation, search/export
  bounds, idempotent replay, tampered replay conflict, missing idempotency
  headers, and missing resources.
- `python3 -m unittest discover -s tests/acceptance` passed all 10 acceptance
  harness tests after the threaded mock-server correction.
- Full operation-level edge-case evidence remains tracked by dimension; the
  current collection and live suites provide complete success smoke coverage,
  while endpoint-specific authorization, malformed-input, timeout, and
  dependency-failure rows remain uneven and are not claimed as complete.

- The public-surface validator now reconciles all 41 REST operations and all 9
  GraphQL root fields against their annotated BFF resolvers. `make contracts`,
  acceptance unit tests, workflow parsing, and `git diff --check` passed.

- The same validator also requires an ordered Bruno request for every REST
  operation. It found missing public requests for sync changes and notification
  inbox; both requests were added with response assertions. This closes an
  inventory gap, but does not by itself prove every coverage dimension in the
  matrix.

- The validator now reports assertion density separately from inventory
  completeness. Endpoint-specific assertions were added to the remaining
  collection requests; the live collection now executes 46 requests with 51
  assertions while retaining dedicated E2E tests for deeper negative and
  failure dimensions.

- Added direct GraphQL HTTP success coverage for `updateGroup` and
  `createExpense`; the focused BFF transport suite now passes 9 tests. The
  coverage document now maps each executable suite to its boundary, dimensions,
  evidence option, and explicit limitation.

### Active finding

The CSV export now handles an incompatible `Accept: application/json` header as
HTTP 406 with an intentionally bodyless response, avoiding a second content-
negotiation failure while preserving RFC 7807 responses for negotiable errors.
The edge suite covers both this 406 case and `maxRows=0` as HTTP 400; the
standalone controller regression and live check both pass.

The latest `make e2e-all` execution passed all four live suites: product
lifecycle, offline replay/cursor recovery, authenticated GraphQL WebSocket
subscription plus optimistic concurrency, and RabbitMQ pause/outbox recovery.
This is local live-integration/E2E evidence, not production-scale, multi-region,
restore, security-scan, or deployment-rollback evidence.

The BFF fanout/controller tests also cover filtering, multiple subscriptions,
bounded queue ordering/pressure, unsubscribe, TTL expiry, user revocation, and
invalidation side effects. `make acceptance-live` independently passed all ten
acceptance journeys against the running stack.

Added `docs/quality/public-interface-operation-matrix.md`, with one row for
each of the 41 OpenAPI operations and explicit current evidence plus remaining
dimension work. This makes incomplete per-endpoint deep coverage visible
instead of hiding it behind suite-level summaries.

Recent verified increments:

- Expense Core group-scoped expense operations now enforce active membership;
  controller tests and live edge checks cover non-member create, list,
  balances, update, and delete behavior.
- Settlement record, suggestions, and reversal operations now enforce active
  membership; focused controller tests and live non-member checks pass.
- Notifications inbox unauthenticated, zero-limit, malformed-cursor, and
  unknown-notification cases are covered at controller and live boundaries;
  unauthenticated inbox listing now maps to an explicit 401 instead of an
  unhandled null-principal failure.
- Group lifecycle and recurring schedule non-member operations are covered by
  valid-input live requests; Accounts lifecycle authentication and Notifications
  preference authentication have live checks.
- The live REST edge suite currently passes 86 checks. The latest focused run
  includes valid invitation claim, invalid-token rejection, replay rejection,
  and post-claim group access. The latest complete verification run passed
  `make check`, 10/10 live acceptance journeys, Bruno 46/46 requests with
  51/51 tests, and all four `make e2e-all` suites.
- These results are local Docker evidence. Real OIDC, production-scale
  capacity, multi-region failover, restore, security scanning, and deployment
  rollback evidence remain environment-dependent.
- Archived groups now reject new expense writes with `ERR_06`/HTTP 409 after a
  live edge test exposed that missing invariant; the fix is covered by both
  `ExpenseControllerTest` and the deployed REST edge suite.
- Recurring schedule endpoints now reject missing principals explicitly before
  membership checks; `RecurringExpenseControllerTest` covers the existing
  group case and the live suite continues to verify non-member denial.
- The live WebSocket suite now verifies malformed subscription validation is
  returned in a `next` payload with a validation error and `complete` frame,
  while the valid subscription remains usable for later invalidation events.
- `groupChanged` now authorizes the requested group through Expense Core before
  subscribing to fanout events; unit and live tests verify a non-member cannot
  observe another group's invalidations.
- Archived groups are now excluded from both JPA and in-memory active group
  listings and member lookup; the live edge suite verifies HTTP 404 lookup and
  absence from the active list after archive.
- Archived group member listings are also hidden with HTTP 404; both persistence
  implementations and the deployed edge suite cover this state invariant.
- Invalid invitation claims are explicitly rejected with HTTP 409 and are now
  covered by the deployed REST edge suite; authenticated successful claim and
  replay, revocation, unauthenticated access, and expiry-boundary validation are
  covered, while elapsed-time expiry remains open. Authorized revocation of an
  unknown token is intentionally mapped to HTTP 409 to avoid token-state
  disclosure.
- GraphQL HTTP live coverage now includes authenticated/unauthenticated access,
  empty group lists, malformed and missing group queries, malformed settlement
  suggestions, invalid group input, non-member group and repayment mutations,
  malformed repayment money, idempotent and tampered expense replays, and
  outbox-visible product side effects.
- The live chaos suite now pauses Expense Core and verifies that the GraphQL
  `groups` query returns a structured error envelope with no fabricated data;
  it restores Expense Core before exercising the RabbitMQ/outbox recovery flow.
- The live WebSocket suite covers authenticated delivery, malformed operation
  frames, non-member denial, disconnect/reconnect with authenticated
  resubscription, completed-subscription filtering, concurrent stale-version
  recovery, and event delivery.
  Duplicate subscription-ID behavior produced no observable frame in an
  isolated probe and remains an explicit protocol gap; replay after reconnect,
  backpressure, and timeout coverage remain environment-dependent.
- Sync endpoints now enforce active group membership before exposing snapshot or
  change feeds; a live authorization probe found the missing check, and both
  `SyncControllerTest` and the deployed REST edge suite cover the regression.
- The BFF fanout HTTP-double fixture now separates the five-second success-path
  budget from its dedicated 100-millisecond timeout assertion and shuts down its
  cached executor after every test. The focused fanout suite and the complete
  parallel Gradle test/check/coverage/package graph passed with forced task
  execution, closing the observed parallel timeout regression without changing
  the production two-second upstream timeout.
- Repository Python tooling now supplies `encoding="utf-8"` for every
  `Path.read_text` call. Both contract validators pass with `PYTHONUTF8=0` on
  Windows, directly covering the earlier CP1252 decoding failure.
- Version and maturity documentation now agrees with the verified Java 25,
  Kotlin 2.4.20, Spring Boot 4.1.1, and Gradle 9.7.1 baseline. API status records
  durable implementation of the 41 REST operations and the current 86 live edge
  checks without presenting that local evidence as production validation.
- GraphQL HTTP transport coverage now exercises an upstream failure or timeout
  for every query and mutation, asserts the standard HTTP 200 error envelope,
  and verifies private upstream details are redacted. Settlement suggestions
  also cover the empty-result boundary. Production retry timing and WebSocket
  pressure/replay semantics remain assigned to QA-08.
- REST controller coverage now verifies Accounts batch maximum-size enforcement
  and duplicate-ID deduplication, member-removal and invite-revocation replay
  conflicts, and idempotent recurring pause/resume transitions. Focused Accounts
  and Expense Core tests passed; live replay confirmation remains open where the
  local Docker daemon is unavailable.
- Settlement transport now covers successful recording, numeric/positive and
  distinct-participant validation, not-found reversal, and idempotent reversal
  replay without overwriting the original reason. Both JPA and in-memory stores
  map missing/cross-group settlements to the governed not-found error, and the
  OpenAPI response model now matches the implemented participant field names.
