meta {
  name: Update Notification Preferences
  type: http
  seq: 4
}

put {
  url: {{baseUrlNotifications}}/notifications/v1/preferences
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("notification preferences update succeeds", function() {
    expect(res.getStatus()).to.eql(204);
  });
}

body:json {
  {
    "emailEnabled": true,
    "pushEnabled": false
  }
}
