meta {
  name: Invalid subject token is rejected by BFF
  type: http
  seq: 22
}

post {
  url: {{baseUrlBff}}/graphql
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{invalidSubjectToken}}
}

body:json {
  {
    "query": "{ groups { id name } }"
  }
}

tests {
  test("invalid subject token is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
