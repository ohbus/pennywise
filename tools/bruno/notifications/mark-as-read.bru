meta {
  name: Mark Notification Read
  type: http
  seq: 2
}

post {
  url: {{baseUrlNotifications}}/notifications/v1/inbox/{{notificationId}}/read
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("unseeded notification read is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
