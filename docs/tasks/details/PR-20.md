# PR-20 — GraphQL abuse controls

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: BFF-06, BFF-07, BFF-09, QA-07
- Owned paths: `app/bff/src/main/kotlin/com/subhrodip/pennywise/bff/config/`, `app/bff/src/main/kotlin/com/subhrodip/pennywise/bff/realtime/`, focused BFF tests, and this task detail

## Objective

Bound GraphQL resource consumption and subscription accumulation so authenticated
clients cannot exhaust BFF CPU, memory, or reactive queues with deeply nested,
high-complexity, or unbounded subscription workloads.

## Acceptance criteria

- GraphQL depth and calculated complexity limits are configurable and enforced
  before resolver execution.
- Per-user subscription counts are bounded and expired subscriptions do not
  consume capacity.
- Queue capacity and subscription TTL remain bounded and configurable.
- Focused tests prove query instrumentation wiring and subscription rejection.

## Validation

- `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon`
- `python3 tools/contracts/validate.py`
- `git diff --check`

## Current increment

Added configurable GraphQL Java max-depth and max-complexity instrumentation,
plus a per-user live-subscription bound with expiry cleanup. BFF compilation and
test compilation pass; transport-level rejection evidence remains open.
