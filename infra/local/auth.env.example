# Generate a unique value per environment; never commit the populated file.
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
PENNYWISE_SECURITY_CREDENTIAL_DIGEST_SECRET=
