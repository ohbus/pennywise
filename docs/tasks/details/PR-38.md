# PR-38 — CI workflow and release-gate parity

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-21, PR-32
- Owned paths: GitHub workflow files, CI operations documentation, and this task detail

## Objective

Make the hosted workflow lint and repository release-gate commands execute
consistently on Windows and CI environments.

## Acceptance criteria

- All workflow YAML files use LF line endings accepted by yamllint.
- The documented release-gate command references the existing validator.
- Security, architecture, SBOM, workflow lint, and release validation commands
  are directly runnable from the repository.

## Validation

- `uvx --from yamllint==1.37.1 yamllint -d '{extends: relaxed, rules: {truthy: disable, line-length: disable}}' .github/workflows`
- `python tools/ops/validate_release_gate.py`
- `git diff --check`

## Current increment

Registered from the failing workstation CI workflow newline check and release
gate command-path mismatch. No implementation claim is made yet.
