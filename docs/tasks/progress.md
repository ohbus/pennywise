# Implementation progress ledger

The registry is the authoritative state machine. This ledger records meaningful
execution checkpoints and evidence; it does not replace task acceptance criteria.

| 2026-09-21 | Production-readiness audit | Reconciled the audit's historical finding descriptions with current implementation evidence and documented the remaining open/partial findings and the no-infrastructure gate | Audit now distinguishes implemented code-level remediation from unverified production-scale, HA/DR, rotation, rollback, supply-chain, and security-review evidence; release remains NO-GO | documentation increment pending commit |

| 2026-09-20 | QA-07 / CI | Fixed the live QA-05 BFF fanout acceptance path and removed current deprecation warnings | Acceptance fault headers now propagate through the BFF reactive context to Expense Core; the E2E job installs uv via setup-uv; `asText()` uses `asString()` and deprecated `UNPROCESSABLE_ENTITY` uses `HttpStatusCode.valueOf(422)`; focused BFF and Expense Core tests, acceptance unit tests, uvx mypy, contract validation, and diff checks pass | live Docker acceptance rerun remains required |
| 2026-09-20 | QA-07 / CI | Completed signed-persona live integration coverage and corrected authenticated notification delivery/Compose container portability | `make acceptance-live` equivalent passed 10/10; REST edge passed 86 checks; product, offline replay, WebSocket/concurrency, and RabbitMQ/outbox chaos suites passed; signed `local-oidc` Bruno passed 71/71 requests and 78/78 tests; `./gradlew.bat test --rerun-tasks --no-daemon` passed 40 actionable tasks; `uvx mypy`, contract/public-surface validators, compileall, and diff checks pass | hosted CI/production-scale QA-08 evidence remains environment-dependent |

| Date | Task(s) | Change | Verification | Commit |
|---|---|---|---|---|
| 2026-09-20 | AUTH-06, OPS-24 | Added readiness healthchecks for all local application containers and changed BFF dependencies from `service_started` to `service_healthy`; diagnosed the live BFF GraphQL failure as an upstream startup race (`accounts-api:8080` connection refused) | Live logs reproduced the race; Compose topology now gates BFF startup on Accounts, Expense Core, and Notifications readiness; full rebuilt GraphQL probe remains to be rerun | pending |
| 2026-09-20 | AUTH-04, AUTH-06 | Rebuilt current application JARs and executed a live signed-token probe against the healthy Compose stack | Keycloak client-credentials token with internal issuer and `pennywise-api` audience authenticated Accounts `/me` (HTTP 200) and Expense Core groups (HTTP 200); BFF GraphQL returned HTTP 400 for the probe and remains open for transport/query diagnosis | pending |
| 2026-09-20 | AUTH-03, AUTH-04 | Centralized OIDC/JWT claim names, algorithm defaults, OAuth rejection codes, and configuration messages in `OidcSecurityConstants`; removed direct protocol literals from shared decoder and validator adapters; corrected servlet principal binding so JWT resource-server subjects reach domain controllers | `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon` passed; Accounts and Expense Core test suites passed; contract validation and `git diff --check` passed; live recheck after rebuilding application images remains pending | pending |
| 2026-09-20 | AUTH-07, OPS-24 | Corrected the CI auth-email envelope fixture key to a valid padded Base64 encoding of exactly 32 bytes; live startup exposed the invalid fixture before any protected request could be verified | Reproduced application startup failure from container logs; corrected workflow fixture and will rerun Compose startup/token-protected request | pending |
| 2026-09-20 | AUTH-04, AUTH-06 | Verified live Keycloak client-credentials token issuance with `pennywise-api` audience and corrected the CI token request to preserve the internal issuer (`http://idp-keycloak:8080`) while connecting through the host-published port | Docker Compose stack reached healthy state; decoded token contains the expected issuer, audience, bearer type, and service-account subject; protected application journey remains in progress | pending |
| 2026-09-20 | AUTH-06, OPS-24 | Fixed the pinned Keycloak Compose healthcheck: the image does not ship `wget`, so the previous probe left the provider permanently unhealthy and blocked `docker compose --wait`; the probe now uses the image-provided Bash TCP path against `/health/ready` | Live Docker startup reproduced the failing `wget` probe; replacement healthcheck and full-stack token journey remain in validation | pending |
| 2026-09-20 | AUTH-06, OPS-24 | Removed the manual CI bearer-token dependency: the imported Keycloak realm now provisions a CI-only confidential service account with an audience mapper; the E2E workflow obtains a short-lived signed client-credentials token and injects it consistently into Bruno, acceptance, REST, and other test scripts | Realm JSON, contracts, Python compilation, and diff checks passed; live Keycloak token acquisition and full Compose E2E remain to be executed in CI or a Docker-enabled workstation | pending |
| 2026-09-20 | AUTH-05, AUTH-06, OPS-24 | Aligned hosted E2E auth with the `local-oidc` Compose profile: workflow Bruno/acceptance/E2E steps now consume a signed `CI_BEARER_TOKEN`, and the acceptance runner no longer hardcodes `test-user` for live calls | Workflow validation and Python compilation are pending hosted confirmation; the workflow now fails explicitly when the signed-token secret is absent instead of sending a placeholder bearer | pending |
| 2026-09-20 | OPS-24, AUTH-07 | Fixed CI configuration and typing regressions: workflow-scoped ephemeral 32-byte Base64 auth secrets now satisfy Compose preflight and local-stack startup; E2E UTF-8 stream configuration uses typed `TextIO` casts accepted by strict mypy; corrected AUTH-07 documentation so focused implementation evidence is not overstated as production readiness | Both Compose configurations rendered with the CI values; `py -3 -m compileall -q tests tools`, `py -3 tools/contracts/validate.py`, and `git diff --check` passed; hosted `uvx` mypy rerun remains required because `uvx` is unavailable in this workstation | pending |
| 2026-09-20 | AUTH-07 | Completed provider-neutral passwordless auth flow (`/auth/login/start`, `/auth/login/verify`, `/auth/token/refresh`, `/auth/logout`), token minting SPI (`IdentityProviderPort`), refresh token rotation with family reuse revocation, outbox-backed encrypted email delivery, and complete test suites | `./gradlew test` passed across all modules; `tools/contracts/validate.py` passed; `tools/contracts/validate_public_surface.py` passed | 880d6d4 |
| 2026-09-20 | AUTH-06 | Verified the pinned Keycloak fixture live: realm import completed and OIDC discovery became available through the local provider hostname/port | Keycloak 26.7.4 started from full Compose; `pennywise` realm imported; discovery endpoint returned HTTP 200; application token/E2E validation remains open | pending |
| 2026-09-20 | AUTH-06 | Added pinned local Keycloak 26.7.4 service with imported realm fixture, alongside meaningful internal hostnames and architecture documentation | Compose render and contract validation passed; real Keycloak startup/token/E2E evidence remains open | 4646416 |
| 2026-09-20 | AUTH-04 | Added shared servlet/reactive OIDC JWT decoder factories and production/staging security chains for all four applications; configured issuer/audience environment keys | `./gradlew.bat compileKotlin --no-daemon` passed; signed-token and real-Keycloak REST/GraphQL/WebSocket evidence remains open | a9909d6 |
| 2026-09-20 | AUTH-03 | Added reusable production/staging OIDC configuration guard to `libs/security`, imported by all four applications, requiring HTTPS issuer and audience; documented meaningful Compose hostname policy | `./gradlew.bat compileKotlin --no-daemon` passed; `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon` passed 4 guard tests; contract validation and diff check passed; JWT/provider E2E remain open | 0b943fe |
| 2026-09-20 | AUTH-02 | Removed Expense Core's implicit `test-user` identity fallback; missing/blank subjects now fail before membership authorization, with focused controller, REST-edge, and Bruno coverage added | Focused Expense Core suite passed 14 tests; contract/public-surface validation passed; live REST-edge/Bruno evidence remains environment-dependent | 87d6736 |
| 2026-09-17 | DOC-01–DOC-08 | Wrote product, architecture, technology, API, quality, operations and task documentation; passed documentation gate | Registry links, dependency IDs and contract JSON validated | d7c4f2c |
| 2026-09-17 | FND-01 | Created Gradle multi-project Pennywise scaffold, four apps, four technical libraries, wrapper, and reserved UI workspace | `./gradlew compileKotlin`, `./gradlew bootJar`, `./gradlew test` | d7c4f2c |
| 2026-09-17 | FND-02, OPS-03 | Added modular development/production Dockerfiles, Compose dependencies, service Compose, production image variables and quickstart | Compose config validation; secrets remain external | d41c0d0 |
| 2026-09-17 | FND-03, QA-02 | Added contract/deployment smoke harness, allocation tests, context test, JaCoCo reporting, and test documentation | `python3 tools/contracts/validate.py`; `tests/e2e/contract-smoke.sh`; `./gradlew test jacocoTestReport` | 1f0b8c8 |
| 2026-09-17 | FND-04 | Added repository Makefile covering doctor, validation, tests, coverage, packaging, Compose, images, production config, smoke, and cleanup | `make help`, `make contracts`, and `make e2e` passed; registry has 28 tasks | 83b0874 |
| 2026-09-17 | CORE-02 | Implemented allocation preview contract, deterministic calculator API, controller and HTTP tests | Contract validator and `./gradlew :app:expense-core:test` passed | 2e52f56 |
| 2026-09-17 | FND-05 | Completed shared REST error flow with stable codes, request correlation and common problem-details response | `./gradlew test jacocoTestReport`; `python3 tools/contracts/validate.py`; `make e2e` passed | 8724fa3 |
| 2026-09-17 | FND-06 | Completed central Gradle dependency/plugin catalog and container version examples | `./gradlew compileKotlin`; `./gradlew test`; `rg ':[0-9]+' --glob '*.gradle.kts'` passed | 8724fa3 |
| 2026-09-17 | DOC-09 | Added high-level, low-level, and component Mermaid source diagrams with reproducible Docker rendering | `docker compose -f infra/docs/docker-compose.yml config --quiet`; `make docs-diagrams`; `python3 tools/contracts/validate.py` passed | d504d13 |
| 2026-09-17 | DOC-10 | Added shared IntelliJ run configurations for all applications, JVM tests, and local infrastructure | `git diff --check`; `python3 tools/contracts/validate.py` passed | d504d13 |
| 2026-09-17 | ACC-01 | Added authenticated Accounts profile contract slice, validation, deletion request acceptance, and controller tests | `./gradlew :app:accounts:test` passed; persistence/OIDC wiring remains explicitly pending | 2b40159 |
| 2026-09-17 | CORE-01 | Added validated group creation/list contract slice with member-scoped adapter and controller tests | `./gradlew :app:expense-core:test` passed; invitation/persistence/locking remain explicitly pending | ef6081f |
| 2026-09-17 | MSG-01 | Added deterministic outbox relay contract slice with ordered claims, lease recovery, attempts, and acknowledgement | `./gradlew :app:expense-core:test` passed; persistence, broker confirms, inbox and retry remain explicitly pending | e46429e |
| 2026-09-17 | BFF-01 | Added nonblocking Expense Core WebClient gateway contract slice with configurable URL and bearer forwarding | `./gradlew :app:bff:test` passed; GraphQL resolver and bounded fanout remain explicitly pending | bee5c37 |
| 2026-09-17 | BFF-01 | Added GraphQL groups query and createGroup mutation entry points over the REST gateway | `./gradlew :app:bff:test` passed; schema resource and transport integration remain explicitly pending | 6028a54 |
| 2026-09-17 | BFF-01 | Wired the reviewed GraphQL contract into BFF resources and configured GraphQL HTTP/WebSocket paths | `./gradlew :app:bff:processResources :app:bff:test`; resource existence check; `python3 tools/contracts/validate.py` passed | 3837f56 |
| 2026-09-17 | CORE-01 | Added expiring single-use invitation create/claim endpoints and extended the Expense Core contract | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py` passed; durable persistence/locking remain pending | c41291b |
| 2026-09-17 | CORE-01 | Added invitation contract coverage and retained explicit limitation that transport claim integration needs a security-enabled test harness | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py`; `git diff --check` passed | f96b994 |
| 2026-09-17 | CORE-03 | Added exact minor-unit settlement recording and idempotent audited reversal domain slice | `./gradlew :app:expense-core:test` passed; REST, persistence, authorization and balance projection remain pending | b37c262 |
| 2026-09-17 | CORE-03 | Added validated REST settlement record and reversal boundary with invalid amount coverage | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py` passed | 01eef67 |
| 2026-09-17 | QUALITY | Replaced fully qualified Kotlin references in recent implementation and test code with explicit imports | `./gradlew :app:accounts:test :app:expense-core:test :app:bff:test`; `git diff --check` passed | 45f0369 |
| 2026-09-17 | ACC-01 | Added provider-neutral OIDC issuer and bounded subject claim validation with tests | `./gradlew :app:accounts:test`; `python3 tools/contracts/validate.py` passed | b556849 |
| 2026-09-17 | ACC-01 | Added idempotent deletion-request state model preserving financial history | `./gradlew :app:accounts:test`; `python3 tools/contracts/validate.py` passed | 7dc6d83 |
| 2026-09-17 | ACC-01 | Added independent export-request lifecycle model with stable IDs and status | `./gradlew :app:accounts:test`; `python3 tools/contracts/validate.py` passed | adac531 |
| 2026-09-17 | CORE-05 | Added deterministic weekly/monthly recurrence date policy with month-end clamp tests | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py` passed | 8ccebfe |
| 2026-09-17 | CORE-05 | Added stable occurrence identity derived from schedule ID and local occurrence date | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py` passed | 29820ce |
| 2026-09-17 | CORE-06 | Added bounded stable expense search filtering and CSV formula-injection escaping | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py` passed | 053835c |
| 2026-09-17 | DOC-12 | Attempted project-wide ktlint integration; withheld broken plugin configuration after Kotlin 2.4.20 compatibility failure | `./gradlew ktlintCheck` failed with `Extensions storage is not registered`; `./gradlew test` and contract validation passed | 6fee281 |
| 2026-09-17 | DOC-13 | Reviewed implemented code, ran full verification, removed obsolete empty package folders, and retained architecture decisions directory with gitkeep | `./gradlew test`; `python3 tools/contracts/validate.py`; `git diff --check` passed | 4a276f6 |
| 2026-09-17 | DOC-11 | Documented endpoint-specific pagination policy and bounded collection requirements | `python3 tools/contracts/validate.py`; `git diff --check` passed | 895063f |
| 2026-09-17 | ACC-01, CORE-04, MSG-01 | Delegated independent implementation increments to registered agents; coordinator reviewed their owned-path changes and retained registry/board ownership | Agent reports and commit reviews complete | 56375d8, f11206e, 14eeb4a |
| 2026-09-17 | ACC-01 | Added durable JPA profile adapter, entity, repository, and Flyway profile migration | `./gradlew :app:accounts:test` passed | 56375d8 |
| 2026-09-17 | CORE-04 | Added monotonic sync revisions, paginated snapshots, tombstones, opaque cursors, expiry, and limits | `./gradlew :app:expense-core:test`; `git diff --check` passed | f11206e |
| 2026-09-17 | MSG-01 | Added retry scheduling, backoff eligibility, rejection handling, and parking semantics | `./gradlew :app:expense-core:test`; `python3 tools/contracts/validate.py`; `git diff --check` passed | 14eeb4a |
| 2026-09-17 | CORE-04, MSG-01, BFF-02 | Delegated independent increments to `/root/sync_rest`, `/root/broker_abstraction`, and `/root/live_updates`; coordinator reviewed owned-path changes and retained tracker ownership | Agent reports, full `./gradlew test`, contract validation, and clean worktree complete | 419b30e, d031cae, e9c99fe |
| 2026-09-17 | BFF-02 | Added bounded thread-safe live-update fanout with scoped subscriptions and unsubscribe behavior | `./gradlew :app:bff:test`; `git diff --check` passed | e9c99fe |
| 2026-09-17 | MSG-01 | Added Notifications inbox deduplication boundary for repeated event IDs | `./gradlew :app:notifications:test`; transactional inbox and broker consumer integration remain pending | 1721d10 |
| 2026-09-17 | NOT-01 | Added authenticated notification preference read/update REST slice with tests | `./gradlew :app:notifications:test` passed; durable storage, inbox listing, rate limits and provider delivery remain pending | fd462ba |
| 2026-09-17 | NOT-01 | Added authenticated member-scoped inbox read endpoint with bounded newest-first page and tests | `./gradlew :app:notifications:test`; `python3 tools/contracts/validate.py` passed | f1c9f0a |
| 2026-09-17 | NOT-01 | Added preference-aware, deduplicated delivery decision logic before provider dispatch | `./gradlew :app:notifications:test`; `python3 tools/contracts/validate.py` passed | 6fdc136 |
| 2026-09-17 | NOT-01 | Added deterministic retry backoff, permanent failure parking, and attempt-limit policy | `./gradlew :app:notifications:test`; `python3 tools/contracts/validate.py` passed | f6652a9 |
| 2026-09-17 | NOT-01 | Added deterministic per-recipient delivery rate limiter with reset window and tests | `./gradlew :app:notifications:test`; `python3 tools/contracts/validate.py` passed | 94e9a22 |
| 2026-09-17 | OPS-04 | Added reusable GitHub Actions CI matrix for all applications/libraries, separate PR/branch/main triggers, and main-only GHCR image publication | Ruby YAML parser loaded all workflows; `python3 tools/contracts/validate.py`; `./gradlew test`; `git diff --check` passed | a290e96 |
| 2026-09-17 | OPS-04 | Updated actions to checkout v5, setup-java v6, build-push v7; added preflight dependency fan-out, concurrency cancellation, and Microsoft OpenJDK | Ruby YAML parser, contract validation, and `git diff --check` passed | 64e5a31 |
| 2026-09-17 | OPS-04 | Named trigger jobs explicitly for pull requests, feature branches, and main container delivery | Ruby YAML parser and `git diff --check` passed | d71769d |
| 2026-09-17 | OPS-05 | Hardened reusable workflow permissions, credential handling, timeouts, cache setup, and matrix parallelism; aligned local Make CI targets | Ruby YAML parsing, `make -n ci`, `make -n ci-e2e`, contract validation, and `git diff --check` passed | 48d3e79, 293e4a1 |
| 2026-09-17 | OPS-06 | Centralized reusable Gradle build caching with safe restore keys and documented Docker layer cache policy | Ruby YAML parsing, `python3 tools/contracts/validate.py`, and `git diff --check` passed | 950351f |
| 2026-09-17 | OPS-01 | Added private-network deployment hardening, readiness, graceful shutdown, structured logging, OTLP guidance, and migration rollout procedures | Production Compose config, contract validation, and `git diff --check` passed | 32ab10f |
| 2026-09-17 | OPS-07 | Added health-checked PostgreSQL and RabbitMQ CI services, local Mailpit health check, and lightweight ubuntu-slim lint job | Workflow YAML parsing, Compose config validation, contract validation, and `git diff --check` passed | ba161e1 |
| 2026-09-17 | QA-01 | Added public-interface acceptance harness with machine-readable blocked scenarios and strict service mode | `tests/acceptance/run.sh`, JSON report parsing, and `git diff --check` passed | d0eb6b1 |
| 2026-09-17 | CORE-01 | Hardened group invitation claims with bounded validation, idempotent linking, and concurrent single-use tests | `./gradlew :app:expense-core:test --no-daemon`; `git diff --check` passed | ca476c7 |
| 2026-09-17 | OPS-02 | Added configurable capacity, recovery restart, and operator-supplied cost drills | Shell syntax, Compose validation, estimator and failure checks passed | 8ba04d7, 006ca1c |
| 2026-09-17 | ACC-01 | Centralized profile OIDC subject and timezone validation across in-memory and JPA stores | Accounts tests, contract validation, and diff checks passed | 9721b92, 69f7c37 |
| 2026-09-17 | BFF-01 | Added bounded nonblocking upstream timeout enforcement and stable HTTP error mapping | BFF tests and `git diff --check` passed | 8815455 |
| 2026-09-17 | FND-07 | Added centrally managed UUIDv7 generator and migrated all production-generated identifiers | UUID dependency resolution, focused module tests, and diff checks passed | b9a250f, 3cf2572 |
| 2026-09-17 | CORE-07 | Added validated expense taxonomy, category filtering, CSV category output, and OpenAPI updates | JSON validation and focused Expense Search tests passed | 5f6997c |
| 2026-09-17 | NOT-01 | Added bounded cursor pagination, malformed cursor rejection, and authenticated inbox validation | Notifications tests and `git diff --check` passed | 7f273be |
| 2026-09-17 | BFF-02 | Added subscription expiry, automatic cleanup, user revocation, and validation | BFF tests, contract validation, and `git diff --check` passed | 742609a |
| 2026-09-17 | CORE-04 | Scoped synchronization revisions and opaque cursors per group, rejecting cross-group cursor replay | `./gradlew :app:expense-core:test --no-daemon`; `git diff --check` passed | 6f671b5 |
| 2026-09-17 | DOC-14 | Evaluated Spotless/ktfmt compatibility; resolved successfully but found 65 existing formatting violations, so no failing gate was enabled | Gradle resolution/execution on Kotlin 2.4.20, Java 25, Gradle 9.7.1 | 42f06e5 |
| 2026-09-17 | DOC-15 | Completed parallel architecture/contract and build/CI drift audits; corrected invalid reusable workflow permission placement | Full Gradle tests, contract validation, YAML parsing, and diff checks passed; findings recorded in docs/reviews | 13413b9, 4c5857b |
| 2026-09-17 | DOC-16 | Reconciled Expense Core settlement/sync contracts, GraphQL operation mapping, and explicit API implementation/persistence status | Contract validation, JSON parsing, full Gradle tests, and `git diff --check` passed | 283aeb3 |
| 2026-09-17 | CORE-08 | Added durable group-scoped settlement JPA/Flyway adapter with pessimistic reversal locking and deterministic in-memory tests | 43 Expense Core tests, dependency resolution, and `git diff --check` passed | de11ffe |
| 2026-09-17 | NOT-02 | Added durable notification preference JPA/Flyway adapter with optimistic versioning and H2 integration coverage | 10 Notifications tests and `git diff --check` passed | 7cd8ebb |
| 2026-09-17 | CORE-09 | Added durable group, membership, and invitation persistence with atomic single-use claims and concurrent idempotent linking | Expense Core and focused concurrency tests, contracts, and diff checks passed | e62dab8 |
| 2026-09-17 | NOT-03 | Added durable inbox persistence, stable cursor ordering, and persistent concurrent event deduplication | 13 Notifications tests and `git diff --check` passed | b14ab0b |
| 2026-09-17 | CORE-10 | Added durable outbox persistence with SKIP LOCKED claims and persisted lease/retry/ack/parking transitions | 49 Expense Core tests passed on rerun; diff check passed | 3b61803 |
| 2026-09-17 | NOT-04 | Added transactional notification consumption with atomic dedupe/effect, duplicate acknowledgement, and failure retry | 16 Notifications tests and diff check passed | f9ecede |
| 2026-09-17 | CORE-05 | Added recurrence schedule and occurrence identity validation with focused tests | Expense Core test initially exposed concurrent search test issue; resolved and full Expense Core tests passed | 2fbd09c, abcc0f1 |
| 2026-09-17 | CORE-06 | Added bounded cursor search, currency totals, safe CSV export, and corrected large export bounds | `./gradlew :app:expense-core:test --no-daemon`; contract validation and diff check passed | f65aa59, abcc0f1 |
| 2026-09-17 | MSG-01 | Added leased outbox publisher orchestration, deterministic metadata, confirmations, retries, and parking | Focused implementation committed; full Expense Core tests passed after test import correction | 57170b8, abcc0f1 |
| 2026-09-18 | NOT-05 | Added RabbitMQ transport listener and schema envelope parser with manual ack, poison pill rejection, and transient failure requeue | 22 Notifications tests, contract validation, and `git diff --check` passed | 6efce73 |
| 2026-09-18 | CORE-11 | Added durable group-scoped synchronization JPA/Flyway persistence adapter with sequential revisions, tombstones, and cursor pagination | 53 Expense Core tests, contract validation, and `git diff --check` passed | b8edae9 |
| 2026-09-18 | CORE-12 | Added outbox background polling relay daemon and configuration with test isolation and scheduled batch publishing | 55 Expense Core tests, contract validation, and `git diff --check` passed | 42166ea |
| 2026-09-18 | FND-02, OPS-03 | Verified Docker runtime for all four services; added kotlin-reflect, flyway-database-postgresql, Spring GraphQL MoneyMinor/DateTime scalars, service-isolated PostgreSQL databases, and fast runtime image targets | Container health checks passed for Accounts (8081), Expense Core (8082), Notifications (8083), BFF (8080); 30 Gradle test tasks passed; contract validation passed | 456197f |
| 2026-09-18 | CORE-13 | Added durable expense ledger JPA entities, Flyway migration V5, atomic multi-payer/allocation and balance postings, and REST endpoints | 62 Expense Core tests passed, full repository test suite passed, OpenAPI contract updated and validated | ea5d287 |
| 2026-09-18 | ACC-02 | Added durable account deletion and export request JPA entities, Flyway migration V2, stores, and services with historical attribution preservation | 12 Accounts tests passed, full repository test suite passed, contract validation and diff check passed | 19d44fe |
| 2026-09-18 | CORE-14 | Added durable expense update, deletion, and zero-sum posting reversal with Flyway V6, optimistic locking, outbox and sync tombstones | 68 Expense Core tests passed, full repository test suite passed, contract validation and diff check passed | 7cb2b52 |
| 2026-09-18 | BFF-03 | Implemented GraphQL BFF resolvers for me, group, createExpense, and recordRepayment via Accounts and Expense Core gateways | BFF and Expense Core tests passed, full repository test suite passed, contract validation and diff check passed | 49a249e |
| 2026-09-18 | CORE-15 | Added durable recurring expense schedules, occurrences, and runner with Flyway V7, idempotency hashing, and worker daemon | 79 Expense Core tests passed, full repository test suite passed, contract validation and diff check passed | 57ded4a |
| 2026-09-18 | QA-03 | Implemented real multi-service acceptance test scenarios (settlement, offline replay, websocket resync) with json reports | Acceptance runner mock test passed (9/9), runner executed cleanly, contract validation and diff check passed | 57ded4a |
| 2026-09-18 | NOT-06 | Implemented SMTP email dispatch adapter with JavaMailSender socket client, retry logic, and Mailpit integration | 29 Notifications tests passed, full repository test suite passed, contract validation and diff check passed | 57ded4a |
| 2026-09-18 | ACC-03 | Exposed GDPR export request REST endpoints (POST /me/export-request, GET /me/export-requests) with OpenAPI declarations | 16 Accounts tests passed, full repository test suite passed, contract validation and diff check passed | 57ded4a |
| 2026-09-18 | CORE-16 | Added debt simplification and settlement suggestions engine with greedy balance matching and REST endpoint | 88 Expense Core tests passed, full repository test suite passed, contract validation and diff check passed | 222149c |
| 2026-09-18 | BFF-04 | Implemented settlement suggestions GraphQL query resolver, SuggestedSettlement schema, and gateway mapping | BFF tests passed, full repository test suite passed, contract validation and diff check passed | 222149c |
| 2026-09-18 | NOT-07 | Connected notification consumer to EmailDispatcher with recipient preferences, fallback, and non-blocking failure isolation | 38 Notifications tests passed, full repository test suite passed, contract validation and diff check passed | 222149c |
| 2026-09-18 | OPS-08 | Hardened Compose dev networking environment variables, added make acceptance-live target and quickstart docs | docker compose config validated, contracts validated, diff check passed | 222149c |
| 2026-09-18 | CORE-17 | Added group member listing endpoint (GET /groups/{id}/members) across JPA and in-memory stores with membership authorization | 84 Expense Core tests passed, full repository test suite passed, contract validation and diff check passed | 673ea07 |
| 2026-09-18 | BFF-05 | Implemented groupChanged GraphQL subscription with reactive multicast sink and mutation event emissions | BFF tests passed, full repository test suite passed, contract validation and diff check passed | 673ea07 |
| 2026-09-18 | NOT-08 | Completed NotificationPreferences and /inbox OpenAPI contract schemas, with 401 unauthenticated rejection validation | Notifications tests passed, full repository test suite passed, contract validation and diff check passed | 673ea07 |
| 2026-09-18 | ACC-04 | Exposed public profile lookup endpoint by account ID (GET /profiles/{accountId}) across JPA and in-memory stores | 22 Accounts tests passed, full repository test suite passed, contract validation and diff check passed | 673ea07 |
| 2026-09-18 | DOC-10 | Repaired IntelliJ local settings, then replaced fragile Spring start-point/module lookup with Gradle `bootRun` launchers | Six XML files parsed; four module-qualified `bootRun` tasks resolved; contracts and diff checks passed | e791d7c, 030d10d |
| 2026-09-18 | ACC-05 | Added bounded batch profile lookup across REST, in-memory, and JPA adapters | `./gradlew :app:accounts:test --no-daemon --rerun-tasks`; contracts and diff checks passed | 20e348f |
| 2026-09-18 | CORE-18 | Added group rename and revision increment slice; task remains open for audit/sync/outbox/concurrency guarantees | `./gradlew :app:expense-core:test --no-daemon --rerun-tasks`; contracts and diff checks passed | db31cba |
| 2026-09-18 | BFF-06 | Added group update mutation and single-group member aggregation slice; task remains open for bounded list resolution and error coverage | `./gradlew :app:bff:test --no-daemon --rerun-tasks`; contracts and diff checks passed | 9988edf |
| 2026-09-18 | NOT-09 | Added subject-scoped idempotent notification mark-as-read endpoint and persistence | Isolated `./gradlew :app:notifications:test --rerun-tasks --no-daemon --stacktrace` passed after one concurrent shared-build worker EOF; contracts and diff checks passed | 5a8fec0 |
| 2026-09-18 | OPS-09 | Added dependency-only, per-application prerequisite, and full-stack Compose workflows with exhaustive Make help and operations guide | All six Compose configurations and exact service sets validated; `make help`; 36 target dry runs; contracts and diff checks passed | ea5c1d8, 05b326b, bcdf107, d417c7b |
| 2026-09-18 | DOC-17A, DOC-17B, DOC-17C, DOC-17 | Audited tracker state, Git/file ownership, implementation/contract drift, reconciled active statuses, and registered 11 follow-up tasks | Full cached suite passed; all four application suites passed in isolated forced reruns; contracts, registry/board state comparison, and diff checks passed | f78f6f7, 3cef5ff, 2fe04d9 |
| 2026-09-18 | DOC-17 | Recorded that the prior multi-commit history was reset/amended into root `4ee7a20`; older ledger hashes are pre-squash references available only in local reflog | `git reflog --date=iso`; `git log --all --graph`; file presence and reachable-history review | 4ee7a20 |
| 2026-09-18 | OPS-11 | Fixed main image publishing by creating an explicit `docker-container` Buildx builder and isolating GHA cache scopes per service | `make workflow-validate`; `python3 tools/contracts/validate.py`; `git diff --check` passed. Hosted rerun pending. | 4786f57 |
| 2026-09-18 | DOC-10 | Replaced Gradle fallback entries with standard IntelliJ Application configurations invoking compiled Kotlin `*ApplicationKt` main classes and imported modules | Six XML files parsed; four application classes compiled; contracts and diff checks passed | b52cc79 |
| 2026-09-18 | DOC-20 | Preserved approved multiline ExpenseCategory formatting as a bounded style increment | `git diff --check` passed | 346055a |
| 2026-09-18 | DOC-22 | Codified mandatory programming principles and fresh-session tracker workflow; refreshed README, API implementation status, implementation plan, and acceptance gate wording | Contract/workflow validation and diff checks pending after final documentation pass | 2b99a09 |
| 2026-09-18 | DOC-22 | Removed remaining scaffold/future wording from technology, product, and quickstart documentation | `python3 tools/contracts/validate.py`, `make workflow-validate`, and `git diff --check` passed | 5d01397 |
| 2026-09-18 | DOC-22 | Closed principles and documentation reconciliation task after tracker update | Registry/board synchronized; 93-task registry and workflow/contracts validation passed | 793c7ea |
| 2026-09-18 | BFF-06, BFF-07 | Completed bounded GraphQL member resolution for group lists and stopped masking member-fetch errors | BFF tests, contract validation, and diff checks passed | pending commit |
| 2026-09-18 | DOC-20 | Added centrally versioned Spotless baseline check that preserves approved ExpenseCategory formatting without enabling a repository-wide rewrite | `./gradlew spotlessCheck --no-daemon` and `git diff --check` passed; full migration remains open | pending commit |
| 2026-09-18 | CORE-22 | Added pessimistic group locking plus transactional sync and `group.renamed.v1` outbox effects for rename updates | Focused `JpaOutboxStoreTest` passed; full Expense Core suite had one existing flaky outbox assertion on first run; contracts/diff pending | pending commit |
| 2026-09-18 | CORE-22 | Added V8 group audit persistence and recorded audit, sync, and outbox effects atomically with rename updates | Expense Core compilation passed; full suite repeated the existing flaky outbox assertion; concurrency evidence remains open | pending commit |
| 2026-09-18 | CORE-22 | Added concurrent JPA rename test verifying serialized revisions 1 and 2 | `./gradlew :app:expense-core:test --tests '*JpaGroupStoreTest' --no-daemon` passed; production PostgreSQL confirmation remains open | pending commit |
| 2026-09-18 | OPS-12 | Isolated GHCR image publishing to `ci-main.yml` and removed `packages: write` job from `_reusable-ci.yml`, resolving GHA permission validation error for branch and PR workflows | `make workflow-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | 8bd5cb1 |
| 2026-09-18 | OPS-13 | Aligned CI triggers, workflows, and operations documentation with master branch (`ci-master.yml`, `branches-ignore: [master]`) | `make workflow-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | b00a2b2 |
| 2026-09-18 | OPS-14 | Enabled E2E smoke checks on feature branch CI (`run_e2e: true` in `ci-branch.yml`) with read-only permissions and no artifact delivery | `make workflow-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | 75112d7 |
| 2026-09-18 | DOC-23 | Mandated continuous documentation updates and Javadoc/KDoc comments in AGENTS.md, working agreement, and coding guidelines | `python3 tools/contracts/validate.py`, `make workflow-validate`, and `git diff --check` passed | 0662a7c |
| 2026-09-18 | QA-06 | Isolated `JpaOutboxStoreTest` from inter-suite outbox pollution with `@BeforeEach` cleanup and added structured KDoc doc comments | `./gradlew :app:expense-core:test --rerun-tasks --no-daemon`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | f8c1ebb |
| 2026-09-18 | OPS-15 | Added CI test result summaries (`test-summary/action@v2`) and XML/HTML artifact uploads with `if: always()`, passed down pre-built JARs, and switched image builds to `Dockerfile.fast` | `make workflow-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | dce826a |
| 2026-09-18 | OPS-16 | Standardized top-level CI environment with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'` in reusable and master image workflows | `make workflow-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | 24b32c0 |
| 2026-09-18 | CORE-24 | Added group-rename boundary, Unicode, malformed JSON, and authorization edge case tests | `./gradlew :app:expense-core:test --rerun-tasks --no-daemon` and `git diff --check` passed | f9d6bc7 |
| 2026-09-18 | BFF-08 | Added bounded group-member fanout tests covering concurrency bounds, ordering, request deduplication, timeouts, and error propagation | `./gradlew :app:bff:test --rerun-tasks --no-daemon` and `git diff --check` passed | e610a20 |
| 2026-09-18 | IDE-01 | Configured `graphql.config.yml` and resource inclusion for IntelliJ schema resolution; eliminated unnecessary constructor `@Autowired` annotations | `./gradlew test --rerun-tasks --no-daemon`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | e7d23c7 |
| 2026-09-18 | DOC-24 | Mandated strict SOLID file separation, comprehensive documentation linking, and pre-implementation documentation review across AGENTS.md, GEMINI.md, and quality guides | `python3 tools/contracts/validate.py` and `git diff --check` passed | 9266de9 |
| 2026-09-18 | CORE-26 | Refactored Expense Core groups and settlements persistence into dedicated SOLID entity, repository, and store files | `./gradlew :app:expense-core:test --rerun-tasks --no-daemon` and `git diff --check` passed | 9055a23 |
| 2026-09-18 | ACC-06 | Refactored Accounts persistence into dedicated SOLID entity, repository, and store files | `./gradlew :app:accounts:test --rerun-tasks --no-daemon` and `git diff --check` passed | 9055a23 |
| 2026-09-18 | NOT-10 | Refactored Notifications persistence into dedicated SOLID entity, repository, and store files | `./gradlew :app:notifications:test --rerun-tasks --no-daemon` and `git diff --check` passed | 9055a23 |
| 2026-09-18 | NOT-11 | Ran `./gradlew :app:notifications:test --no-daemon`; all tests passed | BUILD SUCCESSFUL (task-1836) | 2522e9272099a73260520c98babc99b7da3e7c53 |
| 2026-09-18 | CORE-27 | Refactored Expense Core expenses, sync, and outbox persistence into dedicated SOLID entity, repository, and store files | `./gradlew :app:expense-core:test --rerun-tasks --no-daemon` and `git diff --check` passed | 3a53699 |
| 2026-09-18 | CORE-28 | Refactored Expense Core recurring schedules and occurrences persistence into dedicated SOLID entity and repository files | `./gradlew :app:expense-core:test --rerun-tasks --no-daemon` and `git diff --check` passed | 3a53699 |
| 2026-09-18 | FND-08 | Extracted domain ports, in-memory stores, and decoupled processors into dedicated SOLID files; resolved JPA store bean ambiguity with @Primary | `./gradlew test --rerun-tasks --no-daemon` (30 tasks passed), `python3 tools/contracts/validate.py`, and `git diff --check` passed | 8687a3e |
| 2026-09-18 | QA-05 | Added public-interface edge-case journeys for rollback, concurrency, authorization, BFF fanout failure, and recovery | QA-05 and runner unit suites, contract validation, and diff checks passed; rollback remains blocked without fault injection | 7be4b84 |
| 2026-09-18 | CORE-25 | Added opt-in live-PostgreSQL two-transaction group rename concurrency evidence | PostgreSQL integration test, contract validation, and diff checks passed | 4e406af |
| 2026-09-18 | CORE-18, CORE-22, OBS-01 | Closed group rename hardening and observability tracker items after rollback, PostgreSQL concurrency, and full test evidence | Expense Core/BFF/observability tests, contract validation, and diff checks passed | 6aa8629 |
| 2026-09-18 | BFF-10 | Migrated GraphQL scalar coercion APIs and added malformed/valid MoneyMinor and DateTime tests | Focused BFF scalar tests, contract validation, and diff checks passed | 82a8cee |
| 2026-09-18 | BFF-09 | Added GraphQL transport error tests for upstream authorization, validation, timeout, malformed responses, and error-detail non-leakage | BFF tests, contract validation, and diff checks passed | e42597a |
| 2026-09-18 | CORE-23 | Added exact group rename audit/sync/outbox effect assertions and rollback coverage | Focused JpaGroupStoreTest, contract validation, and diff checks passed | ab8d4fa |
| 2026-09-18 | OBS-01 | Completed MDC request correlation, structured error/request logging, scoped background LoggingContext, and sensitive-data sanitization | `./gradlew :libs:errors:test :libs:observability:test --rerun-tasks --no-daemon` passed; full repository validation pending | pending commit |
| 2026-09-18 | DOC-25 | Reconciled implementation plan and tracker drift: removed duplicate BFF-07, closed BFF-11 status, and documented active hardening/quality gates | `python3 tools/contracts/validate.py` and `git diff --check` passed | pending commit |
| 2026-09-18 | QA-05 | Added explicit public rollback fault fixture and threaded HTTP acceptance server; all five edge-case journeys now pass | `python3 -m unittest tests/acceptance/test_qa05.py`, `python3 -m unittest tests/acceptance/test_runner.py`, and `git diff --check` passed | fe286e8 |
| 2026-09-18 | DOC-20 | Closed the non-breaking Spotless baseline while preserving approved ExpenseCategory formatting; repository-wide migration remains separately scoped | `./gradlew spotlessCheck --no-daemon`, `./gradlew test --no-daemon`, and `git diff --check` passed | tracker reconciliation |
| 2026-09-18 | QA-05 | Attempted live acceptance runner; contract validation passed but service probes were blocked because local BFF and Expense Core ports were offline | `make acceptance-live` exit 2; no Compose services running | environment limitation |
| 2026-09-18 | BFF-06 | Reconciled stale status after bounded member resolution and strict upstream error propagation were completed by BFF-07 | `./gradlew :app:bff:test --no-daemon`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | tracker reconciliation |
| 2026-09-18 | DOC-12 | Replaced incompatible ktlint gate with centrally versioned Spotless/ktfmt baseline and wired `make lint` to contracts, formatting, and Gradle checks | `make lint`, `./gradlew spotlessCheck --no-daemon`, contract validation, and `git diff --check` passed | current increment |
| 2026-09-19 | DOC-18 | Reconciled GraphQL operation mapping, batch profile duplicate semantics, and notification read idempotency across OpenAPI contracts | `python3 tools/contracts/validate.py` and `git diff --check` passed | acd0270 |
| 2026-09-19 | CORE-20 | Implemented recurring schedule management transport, bounded catch-up processing, and pause notifications | `./gradlew :app:expense-core:test --tests "*com.subhrodip.pennywise.expensecore.recurring.*"` passed | 3d1c5fa |
| 2026-09-19 | CORE-21 | Completed persistent authorized search and CSV transport with formula escaping and pagination | `./gradlew :app:expense-core:test --tests "*search*"` and `make check` passed | acd0270 |
| 2026-09-19 | CORE-19 | Completed group lifecycle and membership administration (group archive, named placeholders, soft member removal, invite revocation) | `make check` passed (contracts, Spotless code style, tests, JaCoCo coverage, builds succeeded) | acd0270 |
| 2026-09-19 | DOC-19 | Backfilled legacy tracker ownership and specifications (DOC-08, CORE-03, DOC-11, DOC-15A, DOC-15B) | `python3 tools/contracts/validate.py` and `git diff --check` passed | 452a258 |
| 2026-09-19 | MSG-02 | Delivered committed group changes to every BFF replica via RabbitMQ anonymous queues, deduplication LRU, and fanout | `./gradlew :app:expense-core:test :app:bff:test --no-daemon`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | 2a320ca |
| 2026-09-19 | QA-04 | Integrated authenticated acceptance suite and edge-case journeys with machine-readable passed/failed/blocked reporting | `make acceptance`, `python3 -m unittest discover -s tests/acceptance`, `python3 tools/contracts/validate.py`, and `git diff --check` passed | e3e62eb |
| 2026-09-19 | OPS-10 | Produced public-launch restore rehearsal, recovery drill, measured capacity, and hosting cost evidence | `tests/performance/recovery-drill.sh`, `capacity-smoke.sh`, `cost-estimate.sh`, backup/restore rehearsal, and `git diff --check` passed | 0993510 |


| 2026-09-19 | DOC-26 | Recorded local smoke-demo authentication, BFF/Expense Core response alignment, complete Bruno collection, and operational usage documentation | `make check`, focused application tests, contract validation, `git diff --check`, and live Accounts/BFF GraphQL probes passed | e63ee35 |
| 2026-09-19 | DOC-26 | Fixed QA-05 live acceptance routing, local authorization, rollback/fanout probes, and deterministic mock state; all live journeys now pass | `make acceptance-live`, `python3 -m unittest tests/acceptance/test_qa05.py tests/acceptance/test_runner.py`, focused Expense Core/BFF tests, and `git diff --check` passed | a440146 |

| 2026-09-19 | ERR-01–ERR-12 | Completed governed error-reporting implementation: cataloged service/feature/scenario codes (ERR-01–ERR-12), typed exceptions (ApplicationException), structured security and domain error mappings, per-service migration across Accounts, Expense Core, and Notifications, GraphQL upstream error preservation, negative-path test suite updates, and catalog validation tooling | `python3 tools/contracts/validate.py`, `python3 tools/errors/validate_catalog.py`, `make check`, `make acceptance`, `make bruno-run`, `make observability-validate`, and `git diff --check` passed | current increment |

| 2026-09-19 | OPS-17, OPS-18 | Added service-attributed REST problem responses, Prometheus/Micrometer registry support, actuator metrics/health exposure, bounded service tags, production hardening documentation, and deterministic notification test listener isolation | Notifications tests, contract validation, and diff checks passed; full repository gate pending | pending commit |
| 2026-09-19 | OPS-19 | Added Prometheus scrape configuration, bounded-label alert rules, Grafana service overview dashboard, observability validation target, and release hardening checklist | `make observability-validate`, `python3 tools/contracts/validate.py`, and `git diff --check` passed; deployment drill evidence remains OPS-20 | pending commit |
| 2026-09-19 | OPS-20 | Added `make release-gate` and a standard-library validator for production Compose safety, observability assets, dashboard structure, and release checklist coverage | `make release-gate`, contract validation, and `git diff --check` passed; target-environment restore, security, capacity, and rollback evidence remains required | pending commit |
| 2026-09-19 | OPS-20 | Added dependency-free HTTP throughput/latency probe and tracked-file security-hygiene scan with Make targets | `make security-hygiene`, `make release-gate`, Python compilation, and `git diff --check` passed; environment-specific scans and drills remain required | pending commit |
| 2026-09-19 | OPS-21 | Added modular k6 scripts for Accounts, Expense Core, Notifications, and BFF GraphQL with shared configuration, bounded endpoint tags, provisional thresholds, and safe read scenarios | `make load-k6-validate` and contract validation passed; representative k6 execution remains environment-dependent | pending commit |
| 2026-09-19 | OPS-21 | Added Dockerized `make load-k6` runner so k6 does not need to be installed on the host | `make load-k6-validate` passed; k6 binary and application containers were unavailable locally, so no load result is claimed | pending commit |
| 2026-09-19 | OPS-21 | Executed four concurrent 2-minute k6 read scenarios against the live Docker stack | Accounts 4,795 requests at 39.96/s p95 2.95ms; Expense Core 3,601 at 30.01/s p95 5.00ms; Notifications 2,999 at 24.99/s p95 4.73ms; BFF GraphQL 4,195 at 34.96/s p95 7.44ms; all had 0% failed requests and passed thresholds; local evidence only | pending commit |
| 2026-09-19 | OPS-22 | Added 1M-user workload assumptions, per-service target numbers, infrastructure starting envelope, and mixed k6 baseline profile | `make load-k6-validate` and contract validation pending; production-like soak, burst, failover, recovery, and cost evidence required | pending commit |
| 2026-09-19 | OPS-22 | Ran configurable 10-second 1,000 req/s mixed-profile smoke against the live Docker stack | 10,003 requests, 0% failures; Accounts p95 10.11ms, Expense reads p95 217.10ms, GraphQL p95 24.18ms, Notifications p95 12.47ms; all thresholds passed. The expense-write lane is still a read placeholder, so this is wiring evidence, not write-capacity evidence | pending commit |
| 2026-09-19 | OPS-23 | Registered follow-up for fixture-backed Expense Core mutation load with idempotency, cleanup, and financial reconciliation | Planned; current OPS-22 write lane remains explicitly non-write placeholder traffic | pending |
| 2026-09-19 | OPS-23 | Added isolated k6 Expense Core creation scenario with fixture-group setup, UUID expense IDs, idempotency keys, and write thresholds | Script structure validation pending; cleanup/reconciliation and representative execution remain required | pending commit |
| 2026-09-19 | OPS-23 | Fixed k6 v2 UUID compatibility and added automatic fixture archival in teardown; local interrupted smoke produced successful writes before cancellation and archived its fixture group | Local script check only; no production-capacity evidence claimed | pending commit |
| 2026-09-19 | LOCAL-VERIFY | Made Notifications broker resources self-declaring (durable events exchange, queue, and all-events binding) and enabled the listener for runtime profiles; isolated tests continue to override listener startup | `./gradlew :app:notifications:test --rerun-tasks --no-daemon`, `make check`, `make observability-validate`, `make security-hygiene`, `make release-gate`; fresh host-native services exposed health/Prometheus endpoints, BFF GraphQL `me`/`groups` passed, and local RabbitMQ restart recovered with the Notifications consumer connected | current increment |
| 2026-09-19 | OPS-23 | Executed fixture-backed Expense Core mutation load and reconciled the archived fixture: 250 successful writes, 0% failures, p95 15.57 ms, 250 expenses, 500 postings, 251 outbox records, 251 sync changes, revision 251; duplicate replay produced no additional persistence effects | `DURATION=5s BASE_URL=http://localhost:8082 k6 run tests/load/k6/mutation-expense.js --summary-export=/tmp/k6-mutation-summary.json`; direct PostgreSQL reconciliation and duplicate HTTP replay passed; bounded local evidence only | current increment |
| 2026-09-19 | OPS-19, OPS-20 | Added local Prometheus target configuration and Grafana provisioning for the checked-in datasource and overview dashboard | Prometheus `/-/ready`, API targets 4/4 healthy, query `sum(up{job="pennywise"})=4`; Grafana `/api/health` healthy and dashboard `pennywise-overview` provisioned; `make observability-validate` passed | current increment |
| 2026-09-19 | OPS-20 | Exercised local RabbitMQ dependency loss and recovery with the Notifications consumer | Consumer present before failure, broker container stopped, broker diagnostics recovered, queue returned with one consumer, Notifications readiness remained UP, recovery completed in 11 seconds; local-only dependency drill | current increment |
| 2026-09-19 | OPS-20, QA-04 | Fixed local BFF RabbitMQ credentials so the aggregate health gate reflects the broker dependency; reran authenticated live acceptance | `make acceptance-live` passed all contract, health, group/expense/settlement, offline replay, GraphQL, rollback, concurrency, authorization, fanout, and recovery journeys | current increment |
| 2026-09-19 | DOC-26 | Executed the actual Bruno CLI collection with ordered group, expense, settlement, sync, notification, recurring, quality, and lifecycle flows; made archive sequencing explicit and expense IDs/idempotency keys dynamic | `make bruno-run BRUNO_ENV=local BRUNO_TOKEN=test-user`; 43 requests passed with 16/16 assertions; expected non-positive cases remain profile lookup 404 and mark-as-read without a seeded notification 401 | current increment |
| 2026-09-19 | DOC-26 | Added adaptable Bruno `ci` and staging-template environments, centralized token/base-URL overrides, a repeatable Make runner, and safe dynamic fixture guidance | `git diff --check`, `make compose-config`, and ordered Bruno CLI run passed; no environment secrets committed | current increment |
| 2026-09-19 | OPS-20 | Added a local release verification record separating proven local gates from deployment-specific production blockers | `docs/operations/local-release-checklist.md`; `make release-gate` correctly continues to block missing production restore, alert, secret, ingress, scan, capacity, and rollback evidence | current increment |
| 2026-09-19 | LOCAL-VERIFY | Reworked local Docker Compose to run host-built JARs in JRE images, avoiding Gradle distribution/dependency resolution during container startup; added Docker BFF broker credentials and Expense Core outbox relay flags | `make package`, `docker compose -f infra/local/docker-compose.dev.yml build`, and `up -d --wait` succeeded; all four application containers became healthy and `make acceptance-live` passed | current increment |
| 2026-09-19 | DOC-26 | Added Bruno response assertions for profile, group persistence, expense response shape, GraphQL error absence, notification preferences, authentication challenges, and structured error attribution | Ordered Bruno run: 35 requests passed with 9/9 assertions; quality requests passed with 3/3 assertions | current increment |
| 2026-09-19 | NOT-05, QA-04 | Published a contract-valid local notification event through RabbitMQ, verified unread inbox delivery, marked it read through REST, and verified `read=true` afterward | RabbitMQ publish routed successfully; inbox returned seeded notification; `POST /read` returned 204; follow-up inbox returned `read:true` | current increment |
| 2026-09-19 | OPS-22, OPS-23 | Replaced the 1M k6 profile's write placeholder with fixture-backed UUID/idempotent expense writes and ran a 30-second mixed mutation/read soak | 5-second profile smoke: 5,003 iterations, ~996 req/s, 0% failures, thresholds passed. 30-second concurrent soak: 0% failures but Expense Core p95 exceeded thresholds (reads ~1.70s, writes ~2.05s), k6 exit 99; local saturation limitation recorded | current increment |
| 2026-09-19 | OPS-20 | Exercised Docker-local PostgreSQL, RabbitMQ, and BFF→Accounts dependency loss/recovery | Rabbit consumer 1→outage→1; BFF upstream returned connection failure during Accounts outage and recovered HTTP 200; Expense Core health recovered HTTP 200 after PostgreSQL restart | current increment |
| 2026-09-19 | CORE-20, DOC-26 | Added and executed modular Bruno recurring-schedule journey for create/list/get/update/pause/resume | Docker-local Bruno run passed 17 ordered group/recurring/lifecycle requests with 7 recurring assertions | current increment |
| 2026-09-19 | OPS-23 | Added automated post-run financial and persistence reconciliation tool `tools/ops/reconcile_mutation_fixture.py` and `make load-mutation-check`; verified zero-sum balance invariant, 500 postings for 250 writes, monotonic revisions, and idempotent replay without revision bumps | `make load-mutation-check` passed with 0% failures and verified reconciliation | current increment |
| 2026-09-19 | OPS-20 | Formalized release gate package: repository-owned prerequisites, security hygiene checks, live multi-service acceptance, dependency outage rehearsals, and local release checklist sign-offs | `make release-gate`, `make security-hygiene`, `make acceptance-live`, and `make check` passed | current increment |
| 2026-09-19 | LOCAL-VERIFY, QA-04 | Audited and hardened local Docker backend: fixed Flyway PostgreSQL driver dependency in Expense Core; fixed OutboxPublisher JSON serialization with ObjectMapper; fixed BFF UpstreamExpense and UpstreamSettlement deserialization mappings; created and verified comprehensive multi-service E2E test suite covering entire product journey (profiles, group creation, invites, expenses, zero-sum balances, settlement suggestions, repayments, outbox-to-inbox messaging, offline sync) | `python3 tests/e2e/test_product_journey.py` passed with 100% success; `python3 tests/acceptance/runner.py --require-services` passed 10/10; `make check` passed | d3e67f0 |
| 2026-09-19 | ERR-02, QA-02 | Hardened `GlobalErrorHandler` to strictly map all `ErrorCode` enum variants to valid `problem.schema.json` codes (`VALIDATION_FAILED`, `UNAUTHENTICATED`, `FORBIDDEN`, `NOT_FOUND`, `CONFLICT`, `INTERNAL_ERROR`, `RATE_LIMITED`); added test runner and comprehensive unit test suite in `libs/errors` verifying status codes, payload shapes, and RFC problem schema compliance | `./gradlew :libs:errors:test`, `make check`, and `make e2e-live` passed | 6d5ebad |
| 2026-09-19 | QA-05, OFF-01 | Added offline client sync and replay resilience E2E test suite in `tests/e2e/test_offline_resilience.py`: verifies offline mutation queueing with client-side UUIDs and unique `Idempotency-Key` headers, batch reconnection replay, retransmission idempotency (zero balance changes & duplicate suppression), HTTP 409 conflict on tampered idempotency reuse (`ERR_06`), and incremental sync cursor gap recovery | `python3 tests/e2e/test_offline_resilience.py` passed with 6/6 steps verified | 2a0b949 |
| 2026-09-19 | QA-05, BFF-02 | Added concurrent member edit conflict and real-time GraphQL WebSocket subscription invalidation E2E suite in `tests/e2e/test_concurrency_subscriptions.py`: verifies RFC 6455 + `graphql-transport-ws` protocol subscriptions to `groupChanged(groupId)`, simultaneous race-condition PUT updates resulting in exactly 1 winner (version=2) and 1 HTTP 409 stale-version conflict (`ERR_06`), and stale client recovery with clean re-submission (version=3) | `python3 tests/e2e/test_concurrency_subscriptions.py` passed with all verification checks | 7659ae5 |
| 2026-09-19 | QA-05, OPS-20 | Added message broker outage chaos and transactional outbox recovery E2E suite in `tests/e2e/test_chaos_recovery.py`: verifies ACID isolation of Expense Core when RabbitMQ is paused (100% balance and transaction write integrity with outbox held in `PENDING`), broker unpause healing, outbox relay daemon drain to `PUBLISHED`, and downstream Notifications inbox event delivery | `python3 tests/e2e/test_chaos_recovery.py` passed all fault injection and recovery checks | 333c9b3 |

Record exact command, exit status, report path, dependency/runtime version and
known limitation in the registry before marking a task done. Generated build
outputs, Gradle caches, IDE state, Node modules, `.env` files and credentials are
ignored and must never enter commits. Product behavior cannot be marked covered
until API and E2E journey tests exercise it through public interfaces.

| 2026-09-19 | QA-07 | Registered exhaustive public-interface coverage work, added the contract-driven coverage matrix, corrected Expense Core OpenAPI path-item structure, and added semantic REST public-surface validation | `python3 tools/contracts/validate.py`, `python3 tools/contracts/validate_public_surface.py`, and `git diff --check` passed; operation-level integration matrix and missing edge-case suites remain in progress | current increment |
| 2026-09-19 | QA-07 | Fixed the deterministic acceptance harness concurrency false-block by switching its mock service to `ThreadingHTTPServer` | `python3 -m unittest discover -s tests/acceptance` passed all 10 tests; `make contracts` passed with 40 REST operations; full operation-level matrix remains in progress | current increment |
| 2026-09-19 | QA-07 | Expanded GraphQL HTTP transport coverage for group/settlement queries, create-group/repayment mutations, malformed mutation input, and the existing error envelope cases | `./gradlew :app:bff:test --tests 'com.subhrodip.pennywise.bff.GraphqlHttpTransportTest' --no-daemon` passed 7 tests; WebSocket and full REST operation matrix remain in progress | current increment |
| 2026-09-19 | QA-07 | Reconciled the implemented allocation-preview REST operation into the Expense Core OpenAPI inventory and Bruno collection with exact-total assertions | Contract and diff validation pending; full REST operation matrix remains in progress | current increment |
| 2026-09-19 | QA-07 | Executed the ordered Bruno public-interface collection after adding allocation preview | `make bruno-run BRUNO_ENV=local BRUNO_TOKEN=test-user` passed 44/44 requests; 18/18 displayed tests passed; mark-as-read was intentionally the unseeded negative case, while seeded positive notification read coverage remains in the live journey | current increment |
| 2026-09-19 | QA-07 | Re-executed live GraphQL WebSocket and concurrent expense conflict coverage | `python3 tests/e2e/test_concurrency_subscriptions.py` passed authenticated RFC 6455/`graphql-transport-ws` subscription delivery, concurrent 409 stale-version rejection, and version-3 stale-client recovery | current increment |
| 2026-09-19 | QA-07 | Added a live REST edge-case suite and found a CSV export validation discrepancy for `maxRows=0`; added a focused regression test | `make e2e-rest-edge` reached authentication, malformed-ID, allocation, search-boundary checks, then observed HTTP 500 for export `maxRows=0`; source-level regression validation pending | current increment |
| 2026-09-19 | QA-07 | Centralized live REST edge-test route templates, removed an invalid generic media-negotiation assertion, and confirmed CSV boundary behavior against the rebuilt local service | `./gradlew :libs:errors:test :app:expense-core:test --tests 'com.subhrodip.pennywise.expensecore.search.SearchControllerTest' --no-daemon` passed; `make package`, Expense Core restart, and `make e2e-rest-edge` passed all checks; unsupported `Accept` remains an explicit framework follow-up | current increment |
| 2026-09-19 | QA-07 | Revalidated the complete acceptance harness after the REST edge suite and runtime rebuild | `python3 -m unittest discover -s tests/acceptance` passed all 10 tests; live REST edge checks passed 12/12; generic unsupported `Accept` negotiation remains explicitly unclaimed | current increment |
| 2026-09-19 | QA-07 | Added REST/OpenAPI and GraphQL schema-to-resolver parity checks, then wired semantic contracts, acceptance unit tests, workflow parsing, formatting checks, and the full live E2E stack suite into reusable CI | `make contracts`, `python3 -m unittest discover -s tests/acceptance`, `make workflow-validate`, and `git diff --check` passed locally; hosted live-stack execution remains CI evidence | current increment |
| 2026-09-19 | QA-07 | Made contract validation reconcile every REST operation with an ordered Bruno request; discovered and added missing sync-changes and notification-inbox requests | `make contracts` now reports 41 REST operations, 9 GraphQL root operations, and a valid Bruno REST request inventory | current increment |
| 2026-09-19 | QA-07 | Added direct GraphQL HTTP success coverage for `updateGroup` and `createExpense`, and documented the executable suite-to-dimension/evidence-options matrix with limitations | `./gradlew :app:bff:test --tests 'com.subhrodip.pennywise.bff.GraphqlHttpTransportTest' --no-daemon` passed all 9 tests; `git diff --check` passed | current increment |
| 2026-09-19 | QA-07 | Added assertion-density reporting to public-surface validation so complete REST inventory is not confused with complete endpoint assertions | `make contracts` reports all 41 REST operations and 9 GraphQL root operations; every current Bruno HTTP request is assertion-backed, while deeper endpoint-specific dimensions remain tracked separately | current increment |
| 2026-09-19 | QA-07 | Added endpoint-specific Bruno assertions to every collection HTTP request and corrected three response-shape assumptions discovered by the live run | `make bruno-run BRUNO_ENV=local BRUNO_TOKEN=test-user` passed 46/46 requests with 51/51 tests; search, settlement, and sync response shapes now match the implementation | current increment |
| 2026-09-19 | QA-07 | Executed the complete live E2E suite across product lifecycle, offline replay, GraphQL WebSocket concurrency, and RabbitMQ/outbox recovery | `make e2e-all` passed all four suites, including zero-sum balances, outbox-to-inbox delivery, idempotent replay/conflict, cursor-gap recovery, RFC 6455 subscription delivery, stale-version conflict, and broker recovery; production-scale and deployment evidence remains environment-dependent | current increment |
| 2026-09-19 | QA-07 | Revalidated the authenticated acceptance harness and reconciled deterministic BFF fanout coverage into the matrix | `make acceptance-live` passed all 10 journeys; existing BFF fanout/controller tests cover filtering, multiple subscribers, queue pressure/order, unsubscribe, TTL expiry, revocation, invalid input, and invalidation side effects | current increment |
| 2026-09-19 | QA-07 | Fixed CSV export content negotiation so incompatible `Accept: application/json` returns bodyless HTTP 406 instead of HTTP 500; added controller and live regression coverage | `./gradlew :libs:errors:test :app:expense-core:test --tests 'com.subhrodip.pennywise.expensecore.search.SearchControllerTest' --no-daemon` passed; rebuilt local Expense Core and `make e2e-rest-edge` passed 13/13 checks | current increment |
| 2026-09-19 | QA-07 | Established the repository-wide strongly typed Python rule, added `uv`/`uvx` tooling guidance, annotated Python test/tool entry points, and wired strict mypy to Make/CI | `PATH=/Users/smohanta/.local/bin:$PATH make python-typecheck` passed with no issues across 17 files; acceptance unit tests passed 10/10; `git diff --check` passed | current increment |
| 2026-09-19 | QA-07 | Added the literal operation-level evidence matrix with one row for every 41 REST operations and explicit remaining dimension work | Documentation now links contract inventory, current evidence level, and uncovered per-endpoint dimensions; QA-07 remains in progress until those gaps are reduced | current increment |
| 2026-09-19 | QA-07 | Added live malformed-cursor checks for search, sync snapshot, and sync changes, with centralized route constants | `make e2e-rest-edge` now covers 16 checks when run against the rebuilt local stack; matrix rows for the three operations record malformed-cursor evidence while expiry remains open | current increment |
| 2026-09-19 | QA-07 | Added live expired-cursor checks for sync snapshot and changes and clarified that search cursors are ID-based rather than time-expiring | `make e2e-rest-edge` now covers 18 checks; both sync endpoints reject expired cursors with HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added underlying implementation/controller regression tests for malformed search cursors and expired sync cursors, matching the live edge checks | Focused Expense Core search and sync tests plus live `make e2e-rest-edge` verify the same public behavior at unit/controller and deployed boundaries | current increment |
| 2026-09-19 | QA-07 | Extended the expense authorization regression across listing, balances, update, and deletion, not only creation; added valid-input controller checks and deployed non-member requests | ExpenseController focused tests passed; rebuilt Expense Core and `make e2e-rest-edge` passed 25/25 checks, with HTTP 404 for every non-member expense operation | current increment |
| 2026-09-19 | QA-07 | Added Notifications inbox transport coverage for unauthenticated access, zero limits, malformed cursors, and unknown notification reads; fixed nullable principal handling so unauthenticated inbox requests map to HTTP 401 rather than an unhandled 500 | `./gradlew :app:notifications:test --tests 'com.subhrodip.pennywise.notifications.InboxControllerTest'` passed 10 tests; rebuilt Notifications and `make e2e-rest-edge` passed 29/29 checks | current increment |
| 2026-09-19 | QA-07 | Extended live Accounts coverage for empty and invalid profile patches, malformed profile IDs, and empty/malformed batch requests | `make e2e-rest-edge` passed 34/34 checks; Accounts operation rows now record deployed validation evidence | current increment |
| 2026-09-19 | QA-07 | Added an explicit GraphQL/WebSocket operation matrix for all nine schema roots plus the `graphql-transport-ws` lifecycle, distinguishing resolver/HTTP/E2E evidence from remaining reconnect, authorization, retry, and dependency gaps | Matrix now names each query, mutation, subscription, and transport lifecycle with current evidence and concrete remaining dimensions; no completeness claim made | current increment |
| 2026-09-19 | QA-07 | Closed the settlement controller authorization gap: record, suggestions, and reversal now require active group membership; added controller and live non-member regressions | Settlement controller tests passed; rebuilt Expense Core and `make e2e-rest-edge` passed 37/37 checks, including HTTP 404 for all non-member settlement operations | current increment |
| 2026-09-19 | QA-07 | Extended live group authorization coverage to update, archive, placeholder, member removal, invite creation, and invite revocation using valid non-member requests | `make e2e-rest-edge` passed 43/43 checks; group lifecycle rows now record deployed non-member authorization evidence | current increment |
| 2026-09-19 | QA-07 | Added live missing-authentication checks for Accounts deletion requests, export requests, and export-request listing | `make e2e-rest-edge` passed 46/46 checks; Accounts lifecycle rows now record deployed authentication evidence | current increment |
| 2026-09-19 | QA-07 | Added live missing-authentication checks for Notifications preference read and update operations | `make e2e-rest-edge` passed 48/48 checks; preference rows now record deployed authentication evidence | current increment |
| 2026-09-19 | QA-07 | Extended live recurring-schedule coverage to non-member listing, creation, lookup, and pause requests using valid schedule payloads | `make e2e-rest-edge` passed 52/52 checks; recurring operation rows now record deployed authorization evidence | current increment |
| 2026-09-19 | QA-07 | Re-ran the complete verification package after the authorization and edge-coverage increments | `make check` passed (Gradle tests, JaCoCo, packaging, strict Python typing); `make acceptance-live` passed all 10 journeys; Bruno passed 46/46 requests and 51/51 tests; `make e2e-all` passed product lifecycle, offline replay, WebSocket/concurrency, and RabbitMQ/outbox chaos suites. Evidence is local Docker only; production-scale latency, failover, and real OIDC evidence remain environment-dependent | current increment |
| 2026-09-19 | QA-07 | Found and fixed an archived-group financial-write defect: Expense Core accepted new expenses after archive; added active-group enforcement, controller regression, archive replay coverage, and archived-write E2E coverage | ExpenseController focused tests passed; rebuilt Expense Core and `make e2e-rest-edge` passed 55/55 checks, including archive replay HTTP 409 and archived expense creation HTTP 409 | current increment |
| 2026-09-19 | QA-07 | Hardened recurring schedule authorization so an existing group with a missing principal returns `ERR_03` instead of bypassing membership checks; updated integration tests to use explicit principals | `RecurringExpenseControllerTest` passed; rebuilt Expense Core and `make e2e-rest-edge` passed 55/55 checks | current increment |
| 2026-09-19 | QA-07 | Added WebSocket protocol coverage for a malformed subscription operation; the `graphql-transport-ws` client now records GraphQL validation payloads carried in `next` frames and verifies the stream completes without breaking the valid subscription | `python3 tests/e2e/test_concurrency_subscriptions.py` passed, including malformed-operation error frame, authenticated delivery, concurrent 409 conflict, and stale-client recovery; strict `uvx` mypy passed | current increment |
| 2026-09-19 | QA-07 | Fixed GraphQL WebSocket authorization: `groupChanged` now resolves group access through Expense Core before attaching the fanout stream; added unit and live non-member subscription denial coverage | `GroupGraphqlControllerTest` passed; rebuilt BFF and `python3 tests/e2e/test_concurrency_subscriptions.py` passed malformed-operation, member delivery, non-member denial, concurrency, and stale-client recovery checks | current increment |
| 2026-09-19 | QA-07 | Found and fixed archived-group visibility leakage: active-member group lookup/listing returned archived groups; both JPA and in-memory group stores now expose only ACTIVE groups | Group controller/JPA store tests passed; rebuilt Expense Core and `make e2e-rest-edge` passed 57/57 checks, including archived lookup HTTP 404 and exclusion from active group listing | current increment |
| 2026-09-19 | QA-07 | Extended archived-state visibility coverage to group member listing; JPA and in-memory stores now hide members for archived groups | Group controller/JPA store tests passed; rebuilt Expense Core and `make e2e-rest-edge` passed 58/58 checks, including archived member listing HTTP 404 | current increment |
| 2026-09-19 | QA-07 | Added an explicit invalid invitation-token claim edge check and centralized its route constant; confirmed the existing implementation intentionally maps invalid claims to conflict rather than disclosing token existence | Rebuilt local behavior returned HTTP 409 and `python3 tests/e2e/test_rest_edge_cases.py` passed 59/59 checks; claim expiry, replay, and successful authenticated permutations remain open | current increment |
| 2026-09-19 | QA-07 | Extended invitation lifecycle coverage with valid authenticated claim, membership visibility after claim, and claimed-token replay rejection | `python3 tests/e2e/test_rest_edge_cases.py` passed 63/63 checks; live expiry and revoked-token sequences remain open | current increment |
| 2026-09-19 | QA-07 | Added deployed invitation revocation and revoked-token claim checks, confirming revocation prevents later membership acquisition | `python3 tests/e2e/test_rest_edge_cases.py` passed 66/66 checks; live expiry remains environment/time dependent and open | current increment |
| 2026-09-19 | QA-07 | Added an explicit unauthenticated invitation-claim check to separate authentication failure from invalid-token conflict handling | `python3 tests/e2e/test_rest_edge_cases.py` passed 67/67 checks with HTTP 401 before token processing | current increment |
| 2026-09-19 | QA-07 | Added deployed invitation expiry-boundary validation for the declared 1–168 hour range | `python3 tests/e2e/test_rest_edge_cases.py` passed 69/69 checks; 0 and 169 hours return HTTP 400, while elapsed-time expiry remains open | current increment |
| 2026-09-19 | QA-07 | Added authorized unknown-invite revocation coverage and documented its intentional conflict response, preserving indistinguishable invalid/claimed/revoked token handling | `python3 tests/e2e/test_rest_edge_cases.py` passed 70/70 checks; member revocation of an unknown token returns HTTP 409 | current increment |
| 2026-09-19 | QA-07 | Added GraphQL HTTP transport coverage for malformed JSON, proving the request is rejected before resolver/upstream execution | `./gradlew :app:bff:test --tests 'com.subhrodip.pennywise.bff.GraphqlHttpTransportTest' --no-daemon` passed; malformed `/graphql` payload returns HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added Notifications mark-as-read authentication coverage, separating unauthenticated rejection from authenticated unknown-notification handling | `python3 tests/e2e/test_rest_edge_cases.py` passed 71/71 checks with HTTP 401 for unauthenticated mark-read | current increment |
| 2026-09-19 | QA-07 | Added live authentication coverage for standalone allocation preview, which previously only had validation evidence | `python3 tests/e2e/test_rest_edge_cases.py` passed 72/72 checks; unauthenticated preview returns HTTP 401 | current increment |
| 2026-09-19 | QA-07 | Added deployed group-creation validation coverage for an invalid group kind | `python3 tests/e2e/test_rest_edge_cases.py` passed 73/73 checks; invalid enum input returns HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added deployed unauthenticated group-creation coverage, separating authentication enforcement from request validation | `python3 tests/e2e/test_rest_edge_cases.py` passed 74/74 checks; missing credentials return HTTP 401 | current increment |
| 2026-09-19 | QA-07 | Added deployed placeholder-creation validation coverage for a blank name | `python3 tests/e2e/test_rest_edge_cases.py` passed 75/75 checks; blank placeholder names return HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added deployed group-update validation coverage for a blank name | `python3 tests/e2e/test_rest_edge_cases.py` passed 76/76 checks; blank group names return HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added deployed group-list authentication coverage | `python3 tests/e2e/test_rest_edge_cases.py` passed 77/77 checks; unauthenticated listing returns HTTP 401 | current increment |
| 2026-09-19 | QA-07 | Added deployed group-lookup authentication coverage, distinct from non-member concealment | `python3 tests/e2e/test_rest_edge_cases.py` passed 78/78 checks; unauthenticated lookup returns HTTP 401 | current increment |
| 2026-09-19 | QA-07 | Added deployed archive authentication coverage, distinct from non-member authorization and replay behavior | `python3 tests/e2e/test_rest_edge_cases.py` passed 79/79 checks; unauthenticated archive returns HTTP 401 | current increment |
| 2026-09-19 | QA-07 | Added the Notifications inbox upper pagination boundary check required by the OpenAPI limit maximum | `python3 tests/e2e/test_rest_edge_cases.py` passed 80/80 checks; `limit=101` returns HTTP 400 alongside the existing `limit=0` check | current increment |
| 2026-09-19 | QA-07 | Added malformed notification identifier coverage for mark-as-read | `python3 tests/e2e/test_rest_edge_cases.py` passed 81/81 checks; a non-UUID path identifier returns HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Reconciled the current contract inventory after the latest edge increments | `make contracts` passed: 41 REST operations, 9 GraphQL root operations, 45 Bruno requests with 46 assertion-backed; the operation matrix still records remaining retry, dependency, timeout, and reconnect dimensions explicitly | current increment |
| 2026-09-19 | QA-07 | Added a live GraphQL `group` query with a malformed UUID and asserted the BFF preserves the GraphQL HTTP 200/error-envelope contract | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; `uvx --from mypy==1.17.1 mypy --config-file mypy.ini tests/e2e/test_product_journey.py` passed | current increment |
| 2026-09-19 | QA-07 | Added live unauthenticated GraphQL transport coverage for the `me` query | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; missing credentials return HTTP 401 before resolver execution; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL `group` not-found coverage with a valid nonexistent UUID and asserted the HTTP 200/error-envelope contract | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live malformed-ID coverage for GraphQL `settlementSuggestions` and asserted the BFF error envelope | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL schema-validation coverage for an invalid `createGroup` enum value | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; invalid enum returns a GraphQL error envelope; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live empty-result coverage for the GraphQL `groups` query using a user with no memberships | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; the query returns HTTP 200 with `data.groups=[]`; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL authorization and non-disclosure coverage for the `group` query as a non-member | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; outsider receives an error envelope and no group name is present; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL authorization coverage for non-member `settlementSuggestions` | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; outsider receives a GraphQL error envelope; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Probed duplicate WebSocket subscription IDs on an isolated authenticated connection and rejected an unsupported expectation rather than claiming coverage | The probe produced no observable protocol frame and failed its provisional assertion; the provisional test was removed, the existing WebSocket suite remains unchanged and passing, and duplicate-ID semantics are documented as an open protocol gap | current increment |
| 2026-09-19 | QA-07 | Re-ran the repository-wide verification gate after the GraphQL and WebSocket coverage increments | `make check` passed: contract validation, all Gradle unit/integration tests, JaCoCo reports, packaging, and strict mypy across 17 Python files | current increment |
| 2026-09-19 | QA-07 | Re-ran the complete live E2E bundle after the GraphQL coverage increments | `make e2e-all` passed product lifecycle, offline replay/cursor recovery, WebSocket/concurrency, and RabbitMQ/outbox chaos recovery suites; duplicate WebSocket subscription-ID semantics remain explicitly open | current increment |
| 2026-09-19 | QA-07 | Added malformed membership-ID coverage for authorized group-member removal | `python3 tests/e2e/test_rest_edge_cases.py` passed 82/82 checks; malformed path identifier returns HTTP 400 | current increment |
| 2026-09-19 | QA-07 | Added live non-member authorization coverage for group search and CSV export | `python3 tests/e2e/test_rest_edge_cases.py` passed 84/84 checks; both private read paths return HTTP 404 to an outsider | current increment |
| 2026-09-19 | QA-07 | Found and fixed a sync authorization defect: non-members could read group snapshot/change feeds; added repository-backed membership enforcement and controller regression coverage | `SyncControllerTest` passed; rebuilt Expense Core (`bootJar` + Docker refresh) and `python3 tests/e2e/test_rest_edge_cases.py` passed 86/86 checks with HTTP 404 for both outsider sync reads; first post-restart probe hit a transient connection reset before the readiness retry | c7fcc16 |
| 2026-09-19 | QA-07 | Reconciled the final post-fix verification evidence and REST edge-check count | `make check` passed (contract validation, Gradle tests, JaCoCo, packaging, strict Python typing); `python3 tests/e2e/test_rest_edge_cases.py` passed 86/86; `git diff --check` passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL WebSocket disconnect/reconnect and authenticated resubscription coverage | `uvx --from mypy==1.17.1 mypy --config-file mypy.ini tests/e2e/test_concurrency_subscriptions.py` passed; `python3 tests/e2e/test_concurrency_subscriptions.py` passed through reconnect, delivery, and concurrent conflict recovery | current increment |
| 2026-09-19 | QA-07 | Reconciled QA-07 detail wording with the new WebSocket reconnect evidence | `git diff --check` passed; remaining replay-after-reconnect, malformed-frame, duplicate-ID, timeout, and backpressure dimensions remain explicitly documented | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL WebSocket completion/unsubscribe filtering coverage alongside reconnect/resubscription | Strict mypy passed; `python3 tests/e2e/test_concurrency_subscriptions.py` passed and verified a completed subscription received no later invalidation | current increment |
| 2026-09-19 | QA-07 | Re-ran the complete live E2E bundle after WebSocket completion and reconnect coverage changes | `make e2e-all` passed: product lifecycle, offline replay/cursor recovery, WebSocket/concurrency with reconnect and unsubscribe filtering, and RabbitMQ/outbox outage recovery; evidence is local Docker only | current increment |
| 2026-09-19 | QA-07 | Corrected coverage documentation that conflated tracked WebSocket broker-loss dimensions with verified live evidence | `git diff --check` passed; documentation now explicitly limits live WebSocket claims to handshake, malformed operations, filtering, authorization, reconnect/resubscribe, unsubscribe, and delivery | current increment |
| 2026-09-19 | QA-07 | Re-ran the assertion-backed Bruno contract collection after the latest E2E harness changes | `make bruno-run BRUNO_ENV=local BRUNO_TOKEN=test-user` passed 46/46 requests and 51/51 tests against the local stack | current increment |
| 2026-09-19 | QA-07 | Re-ran the repository validation gate during the completion audit | `make check` passed: contract validation, Gradle tests, JaCoCo, packaging, formatting, and strict mypy; repository remains free of diff-check errors | current increment |
| 2026-09-19 | QA-07 | Audited hosted CI against the QA-07 validation package | `.github/workflows/_reusable-ci.yml` includes contract/public-surface validation, diff/acceptance/workflow checks, strict mypy, Gradle test/check/JaCoCo/package jobs, and all live acceptance/Bruno/product/offline/concurrency/chaos/REST-edge suites when E2E is enabled; no workflow drift found | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL upstream dependency-failure coverage to the chaos suite | `uvx --from mypy==1.17.1 mypy --config-file mypy.ini tests/e2e/test_chaos_recovery.py` passed; `python3 tests/e2e/test_chaos_recovery.py` passed Expense Core pause/error-envelope/no-fabricated-data/recovery and RabbitMQ/outbox recovery | current increment |
| 2026-09-19 | QA-07 | Strengthened GraphQL outage coverage with public-boundary recovery assertion | Strict mypy passed; live chaos suite re-passed and verified `groups` returned the provisioned group after Expense Core restoration before the broker outage drill | current increment |
| 2026-09-19 | QA-07 | Reconciled the operation matrix with the live GraphQL outage/recovery evidence | `git diff --check` passed; the `groups` row now records both the structured outage error and successful public-boundary recovery | current increment |
| 2026-09-19 | QA-07 | Re-ran the complete live E2E bundle after GraphQL dependency outage/recovery coverage was strengthened | `make e2e-all` passed all four suites: product lifecycle, offline replay/cursor recovery, WebSocket/concurrency, and GraphQL Expense Core plus RabbitMQ/outbox recovery | current increment |
| 2026-09-19 | QA-07 | Hardened the WebSocket/concurrency E2E HTTP helper with a centralized ten-second request timeout | Strict mypy passed; `python3 tests/e2e/test_concurrency_subscriptions.py` passed through reconnect, unsubscribe, delivery, and conflict recovery; endpoint timeout semantics remain separately tracked | current increment |
| 2026-09-19 | QA-07 | Added a ten-second setup timeout to the raw RFC 6455 client before switching to blocking WebSocket event reads | Strict mypy passed; live WebSocket/concurrency E2E passed through handshake, reconnect, unsubscribe, delivery, and conflict recovery | current increment |
| 2026-09-19 | QA-07 | Updated E2E operations documentation for GraphQL outage/recovery checks and bounded client setup timeouts | `git diff --check` passed; `tests/e2e/README.md` now documents the fault sequence, timeout behavior, and distinction from application-level timeout evidence | current increment |
| 2026-09-19 | QA-08 | Registered a non-overlapping follow-up for production and unresolved WebSocket protocol evidence | Added `docs/tasks/details/QA-08.md` and `docs/quality/production-validation.md`; registry and board now record QA-08 as planned with QA-07/OPS-20 dependencies and explicit validation artifacts | current increment |
| 2026-09-19 | QA-07 | Revalidated acceptance and contract-shaped request collections after the cumulative coverage work | `make acceptance-live` passed all 10 journeys; `make bruno-run` passed 46/46 requests and 51/51 tests; the attempted alias `make bruno` is not a repository target | current increment |
| 2026-09-20 | QA-05 | Fixed live acceptance authentication propagation: the QA-05 rollback, concurrency, BFF fanout, and recovery probes now receive the CI-supplied signed bearer token while the authorization probe remains intentionally unauthenticated | `python -m unittest tests.acceptance.test_qa05 tests.acceptance.test_runner.RunnerTest.test_all_scenarios_pass_against_mock_services` passed (2 tests); `py -3 tools/contracts/validate.py` passed; `py_compile` and `git diff --check` passed; hosted/live Docker acceptance remains to be rerun | pending CI verification |
| 2026-09-19 | QA-07 | Audited hosted CI wiring against the requested quality gates | `.github/workflows/_reusable-ci.yml` runs contract/static validation, diff/format checks, strict mypy, JVM tests/checks/JaCoCo, application packaging, and—when enabled for PR/master—the complete acceptance, Bruno, REST-edge, product, offline, WebSocket/concurrency, and chaos E2E suites; no workflow change was required | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL authorization coverage for a non-member `updateGroup` mutation | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; outsider mutation returns a GraphQL error envelope without changing group state; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL `createExpense` idempotency coverage by replaying the exact mutation and key | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; replay returned the original expense identity; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL `createExpense` tampered-idempotency conflict coverage | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; changing the description under the same key returns a GraphQL error envelope; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL authorization coverage for non-member `recordRepayment` | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; outsider repayment returns a GraphQL error envelope without recording a settlement; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live GraphQL malformed-money validation coverage for `recordRepayment` | `python3 tests/e2e/test_product_journey.py` passed the complete 11-step lifecycle; non-numeric money returns a GraphQL error envelope; strict mypy passed | current increment |
| 2026-09-19 | QA-07 | Added live cross-user authorization checks for group and member reads, aligned with existing GroupController non-member regression tests | `make e2e-rest-edge` now covers 20 checks; a non-member receives HTTP 404 for both group and membership reads | current increment |
| 2026-09-19 | QA-07 | Closed the Expense Core controller authorization gap: create, update, delete, list, and balance now verify active group membership; added an underlying controller regression and live non-member expense-create check; documented the complete test layout and operating procedure | ExpenseController focused tests passed; `uvx --from mypy==1.17.1 mypy --config-file mypy.ini tests tools` passed; rebuilt Expense Core and `make e2e-rest-edge` passed 21/21 including HTTP 404 for non-member expense creation; new `docs/quality/test-operations-guide.md` explains layers, commands, CI, evidence dimensions, and contribution workflow | current increment |
| 2026-09-19 | OPS-24 | Removed the lightweight CI Ruby assumption and the E2E job's duplicate PostgreSQL/RabbitMQ service containers | Contract/public-surface validation, six Compose config renders, available PyYAML workflow parsing, and `git diff --check` passed; exact `uvx` and live Docker startup were unavailable locally and remain hosted-CI evidence | current increment |
| 2026-09-19 | QA-07 | Stabilized the BFF parallel fanout fixture with separate success/timeout budgets and deterministic executor shutdown | Focused `BffFanoutTest` passed; full `test check jacocoTestReport bootJar --parallel --rerun-tasks --no-daemon` passed 53 executed tasks in 1m56s | current increment |
| 2026-09-19 | QA-07 | Made every Python tooling `Path.read_text` decode UTF-8 explicitly and reconciled runtime/API maturity documentation | With `PYTHONUTF8=0`, contract validation passed for 140 tasks, 41 REST operations, 9 GraphQL operations, and the Bruno inventory; error-catalog and release-asset validation passed; stale-version searches and `git diff --check` passed | current increment |
| 2026-09-19 | QA-07 | Completed local GraphQL HTTP failure-dimension coverage across every query and mutation, including redaction and empty settlement suggestions | Focused `GraphqlHttpTransportTest` passed 13/13 tests; production retry timing and WebSocket pressure/replay evidence remain explicitly assigned to QA-08 | current increment |
| 2026-09-20 | QA-07 | Added REST boundary coverage for batch limits/deduplication, membership and invite replay conflicts, and recurring pause/resume idempotency | Focused `ProfileControllerTest`, `GroupControllerTest`, and `RecurringExpenseControllerTest` passed with 14 Gradle tasks executed; Docker was unavailable for new live replay confirmation | current increment |
| 2026-09-20 | QA-07 | Reconciled settlement response contracts and replaced unknown/cross-group reversal 500s with structured not-found behavior; added record/validation/reversal/replay transport coverage | Focused `SettlementControllerTest` and `JpaSettlementStoreTest` passed with 9 Gradle tasks executed; contract and live-stack validation follow | current increment |
| 2026-09-20 | QA-07 | Added explicit expense-list pagination bounds and aligned the REST contract with the implementation | `./gradlew.bat :app:expense-core:test --tests '*ExpenseControllerTest' --rerun-tasks --no-daemon` passed; invalid limits 0 and 101 return structured HTTP 400 validation errors; production dependency failure evidence remains QA-08 | current increment |
| 2026-09-20 | QA-07 | Re-ran the complete Expense Core test suite after the pagination contract change | `./gradlew.bat :app:expense-core:test --no-daemon` passed; all Expense Core tests remain green | current increment |
| 2026-09-20 | QA-07 | Reconciled search and CSV-export matrix rows with existing filter, pagination, safety, and media-negotiation tests | `SearchControllerTest` covers query/currency/category filters, cursor totals, malformed cursors, export bounds, formula-injection protection, and 406 negotiation; production dependency failures remain QA-08 | current increment |
| 2026-09-20 | QA-07 | Re-ran the repository-wide JVM quality and packaging gate after cumulative API coverage changes | Contract/public-surface validators passed; `./gradlew.bat test check jacocoTestReport bootJar --parallel --no-daemon` passed with 53 actionable tasks; Docker-backed QA-08 remains unavailable | current increment |
| 2026-09-20 | QA-07 | Repeated the stabilized BFF parallel-fanout test five times with clean rebuilds | `./gradlew.bat :app:bff:test --tests '*BffFanoutTest' --rerun-tasks --no-daemon` passed in all five runs; no timing or executor-leak failure reproduced | current increment |
| 2026-09-20 | QA-07 | Revalidated all Python contract, catalog, security-hygiene, and release-gate tooling with locale-independent decoding | With `PYTHONUTF8=0`, every validator exited successfully; explicit UTF-8 reads remain effective and release gates correctly report environment-only restore/security/capacity/rollback requirements | current increment |
| 2026-09-20 | QA-07 | Reconciled Accounts profile/export rows with controller evidence already present in the focused suite | `ProfileControllerTest` covers authenticated profile shape, validated update fields, empty-patch validation, export listing authentication/invalid-subject handling, and profile not-found behavior; service-failure and pagination/load evidence remain open | current increment |
| 2026-09-20 | QA-07 | Reconciled Notifications preference rows with focused controller evidence | `PreferenceControllerTest` covers authenticated defaults, update/read persistence, unauthenticated access, and blank-subject rejection for both operations; persistence-failure evidence remains open | current increment |
| 2026-09-20 | QA-07 | Reconciled recurring-schedule operation rows with the existing lifecycle controller suite | `RecurringExpenseControllerTest` covers create/get/list/update/pause/resume, unknown group/schedule 404s, and missing-principal rejection; production dependency-failure evidence remains QA-08 | current increment |
| 2026-09-20 | QA-07 | Reconciled group and membership rows with the focused controller lifecycle suite | `GroupControllerTest` covers create/list/get/update/archive, invalid payloads, non-member/not-found authorization, revision increments, member listing, placeholder claim, soft removal, and invite revocation; production dependency-failure evidence remains QA-08 | current increment |
| 2026-09-20 | QA-07 | Reconciled allocation-preview coverage with deterministic calculator/controller tests | `AllocationPreviewControllerTest` and `AllocationCalculatorTest` cover exact deterministic allocations, total preservation, invalid participant lists, duplicate/empty inputs, and invalid percentages; production dependency-failure evidence remains QA-08 | current increment |
| 2026-09-20 | QA-07 | Reconciled archive and placeholder rows with group lifecycle evidence | `GroupControllerTest` covers archive replay/archived mutation blocking and placeholder creation with targeted invite binding; production dependency-failure evidence remains QA-08 | current increment |
| 2026-09-20 | QA-07 | Hardened the live REST edge harness for Windows console encodings and reran the full local edge suite | `tests/e2e/test_rest_edge_cases.py` now explicitly configures UTF-8 stdout; local Compose stack passed all 86/86 REST edge checks, including invite expiry/replay, archive behavior, allocation, sync cursors, search/export, and expense idempotency | current increment |
| 2026-09-20 | QA-07 | Hardened the WebSocket/concurrency E2E harness for Windows console encodings and reran its live suite | `tests/e2e/test_concurrency_subscriptions.py` explicitly configures UTF-8 stdout/stderr; local stack passed malformed-operation handling, non-member rejection, reconnect/resubscription, delivery, and concurrent 409 conflict recovery | current increment |
| 2026-09-20 | QA-07 | Hardened the chaos/recovery E2E harness for Windows console encodings and reran its live outage drill | `tests/e2e/test_chaos_recovery.py` explicitly configures UTF-8 stdout/stderr; local stack passed GraphQL dependency outage/recovery, RabbitMQ pause, transactional outbox draining, and notification delivery | current increment |
| 2026-09-20 | QA-07 | Hardened product-lifecycle and offline-replay E2E harnesses for Windows console encodings and reran both live suites | `test_product_journey.py` passed the complete 11-step lifecycle; `test_offline_resilience.py` passed queued replay, idempotent retransmission, conflict detection, and sync-gap recovery on the local stack | current increment |
| 2026-09-20 | QA-07 | Re-ran the assertion-backed Bruno REST collection against the refreshed local stack | Bruno passed 46/46 requests and 51/51 tests across Accounts, BFF, Expense Core, Notifications, lifecycle, and quality collections | current increment |
| 2026-09-20 | OPS-24 | Pinned the Bruno runner to the v4 CLI baseline | `@usebruno/cli@4.1.0` is now explicit in the Make runner and documented for local/CI parity; package metadata reports version 4.1.0 | current increment |
| 2026-09-20 | AUTH-06 | Externalized local OIDC Compose configuration and added a dedicated token-injection Bruno environment | `docker compose -f infra/local/docker-compose.yml -f infra/local/docker-compose.dev.yml config --quiet`, `git diff --check`, and `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon` passed; authenticated full-stack E2E token journeys remain open | current increment |
| 2026-09-20 | AUTH-04 | Added reusable bounded provider-qualified subject validation to servlet and reactive OIDC decoder policies | `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon` passed; coverage includes valid, missing, blank, whitespace-containing, and overlong subjects; `./gradlew.bat compileKotlin --no-daemon` remains the next clean-build check; algorithm, token-type, signed-token, rotation, and boundary E2E evidence remain open | current increment |
| 2026-09-20 | AUTH-04 | Added configurable asymmetric JWT algorithm allow-list and recorded RFC 9700 OAuth flow constraints | `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon` and `./gradlew.bat compileKotlin --no-daemon` passed; direct tests cover configured, unconfigured, blank, and symmetric algorithm cases; real signed-token and full OAuth flow evidence remain open | current increment |
| 2026-09-20 | AUTH-06 | Enforced environment-parity decision and changed local Keycloak fixture from password grant to authorization code plus PKCE | Realm JSON now disables direct access grants, enables standard flow, and requires `S256`; documentation states local/staging/production must share the same auth flow and validation policy; real browser/code-flow E2E remains open | current increment |
| 2026-09-20 | AUTH-05 | Removed arbitrary-bearer local passthrough security configurations and made local-oidc the only supported local application profile | Four local passthrough security classes removed; native operations now require `local-oidc`; compilation and local OIDC E2E are pending validation in this increment; email normalization is specified in AUTH-07 before implementation | current increment |
| 2026-09-20 | AUTH-07 | Added the reusable Accounts `EmailAddress` authentication value object with canonical normalization | `./gradlew.bat :app:accounts:test --tests '*EmailAddressTest' --rerun-tasks --no-daemon` and `./gradlew.bat compileKotlin --no-daemon` passed; tests cover trim/case folding, IDN domain conversion, malformed input, local-part and total-length bounds; endpoint/persistence integration remains open | current increment |
| 2026-09-20 | AUTH-07 | Defined the provider-neutral Pennywise-owned passwordless API contract | `docs/security/passwordless-api-contract.md` specifies login start/verify, rotating refresh, logout, anti-enumeration, email normalization, cookie/CSRF, PKCE, and environment-parity requirements; runtime endpoint and persistence implementation remains open | current increment |
| 2026-09-20 | AUTH-07 | Added provider-neutral one-time credential generation and HMAC-at-rest digest ports | `./gradlew.bat :app:accounts:test --tests '*OneTimeCredentialIssuerTest' --rerun-tasks --no-daemon` and `./gradlew.bat compileKotlin --no-daemon` passed; tests cover entropy, digest separation, lifetime/attempt bounds, and weak-secret rejection; atomic persistence, throttling, delivery, and endpoint integration remain open | current increment |
| 2026-09-20 | AUTH-07 | Added Flyway schema for hashed one-time login credentials and rotating refresh-token session families | V4 migration stores digests, expiry, attempt/consumption state, family/revocation/replacement metadata, and bounded device/client fields; raw credentials and access tokens are excluded; repository adapter and migration integration tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Added Accounts JPA persistence ports/entities for atomic credential consumption and refresh-family rotation/revocation | Full `./gradlew.bat :app:accounts:test --no-daemon` passed; repositories expose conditional command operations so expired, consumed, revoked, or concurrently replaced credentials cannot both succeed; dedicated repository concurrency tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Added Flyway/JPA integration tests for one-time redemption and refresh-session state transitions | `./gradlew.bat :app:accounts:test --no-daemon` passed; `AuthPersistenceTest` proves single-consumer, expiry rejection, one-time rotation, and family revocation behavior against the real test persistence context | current increment |
| 2026-09-20 | AUTH-07 | Added service orchestration for canonicalized credential issuance and generic atomic verification outcomes | Accounts compilation and persistence suites cover the underlying behavior; a Kotlin/Mockito nullability issue made a mock-only service test invalid and it was removed rather than weakening non-null repository contracts; endpoint wiring and delivery adapter remain open | current increment |
| 2026-09-20 | AUTH-07 | Added fail-closed Spring profile wiring for credential-digest cryptography | Compose config rendered successfully with an injected base64 secret; full Accounts test suite passed; missing/weak `PENNYWISE_SECURITY_CREDENTIAL_DIGEST_SECRET` is rejected in `local-oidc`, `staging`, and `production`; secret rotation and endpoint delivery remain open | current increment |
| 2026-09-20 | AUTH-07 | Added deterministic login abuse policy for canonical-key request windows and resend cooldowns | `./gradlew.bat :app:accounts:test --tests '*LoginAbusePolicyTest' --no-daemon` passed; policy covers first request, cooldown denial, exhausted window, reset, and invalid configuration; distributed atomic state adapter and endpoint integration remain open | current increment |
| 2026-09-20 | AUTH-07 | Added durable rate-limit bucket schema/entity/conditional repository port | V5 migration stores only HMAC key digests and bounded window counters; raw email/network identifiers are excluded; policy integration, bucket creation/reset, and concurrency tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Added opaque HMAC rate-limit key derivation for canonical email and trusted network partitions | Focused key-derivation tests cover canonical equivalence, partition separation, and invalid partition rejection; controller/network extraction and atomic bucket acquire integration remain open | current increment |
| 2026-09-20 | AUTH-07 | Added database-native atomic rate-limit bucket acquisition service | PostgreSQL upsert handles first request, reset, cooldown, maximum count, and concurrent conflicts in one statement; service wiring and PostgreSQL concurrency tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Added provider-neutral outbound authentication-email port and message model | Accounts now has link/code templates and generic delivery outcomes without SMTP, Mailpit, Keycloak, or managed-provider coupling; adapter/event wiring and credential redaction tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Added provider-neutral `LoginStartService` orchestration | Service performs atomic rate-limit acquisition, credential issuance, delivery-port handoff, and generic accepted outcomes for invalid/throttled/delivery-failure cases; HTTP adapter, concrete Notifications delivery, and end-to-end tests remain open | current increment |
| 2026-09-20 | AUTH-07 | Completed authentication readiness audit | Confirmed local Keycloak is correctly bounded as a replaceable OIDC provider fixture, while Pennywise-owned login, refresh/logout runtime endpoints, concrete delivery integration, public contracts, authorization negative-path matrix, and live E2E evidence remain incomplete; recorded AUTH-07A through AUTH-07F implementation plan in docs/security/authentication-readiness-review.md | audit evidence |
| 2026-09-20 | AUTH-07A | Added provider-neutral passwordless REST contract and endpoint constants | Accounts OpenAPI schema validation and `libs:ids:test` pass; the stricter public-surface gate correctly reports four missing Bruno requests and evidence-matrix rows, which remain required before AUTH-07A closes; runtime controller/wiring remains gated on delivery and rate-limit adapters | partial contract increment |
| 2026-09-20 | AUTH-07B | Confirmed auth-email delivery ownership boundary | Expense Core outbox is service-private; documented an Accounts-owned transactional auth-email outbox and versioned Notifications handoff with explicit prohibition on raw credentials in generic outbox/inbox payloads | architecture decision |
| 2026-09-20 | AUTH-07B | Added versioned auth-email event payload contract | `auth-email-requested.v1` bounds recipient/template/expiry and requires an encrypted credential field; plaintext fallback and key-management implementation remain blocked by design until the protected delivery adapter is added | contract validation |
| 2026-09-20 | AUTH-07B | Added AES-GCM credential envelope protector | 256-bit key requirement, fresh nonce, versioned URL-safe envelope, authenticated recipient/template context, tamper/context/malformed-input rejection, and invalid-key fail-closed tests pass in `:app:accounts:test`; outbox wiring and Notifications consumption remain open | implementation increment |
| 2026-09-20 | AUTH-07B | Added fail-closed envelope-key configuration | Production-like Accounts profiles and local Compose now require `PENNYWISE_SECURITY_AUTH_EMAIL_ENVELOPE_KEY`; quickstart and task documentation define exact 32-byte Base64 handling with no default | configuration increment |
| 2026-09-20 | AUTH-07B | Added Accounts-owned auth-email outbox persistence | Flyway V6 and JPA boundary persist idempotent event metadata and encrypted credential envelopes separately from Expense Core; Spring persistence suite passes, while claim/publish/consumer wiring remains open | persistence increment |
| 2026-09-20 | AUTH-07B | Added bounded auth-email outbox leasing | Repository row-lock claim and service lease state prevent duplicate active claims and increment attempts; persistence tests pass, while broker publication, failure transitions, and Notifications consumption remain open | concurrency increment |
| 2026-09-20 | AUTH-07B | Added conditional outbox lifecycle transitions | Acknowledge/retry/park operations are claim-conditional and clear the JPA context after mutation; persistence suite passes after catching and fixing stale entity-state behavior | lifecycle increment |
| 2026-09-20 | AUTH-07B | Added disabled-by-default Accounts RabbitMQ publisher | Accounts claims protected events, publishes versioned envelopes, acknowledges only after send, and retries failures; Accounts test suite passes, while Notifications auth-email consumer/decryption remains open | broker adapter increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Completed live BFF bearer propagation through the OIDC boundary | Rebuilt `local-oidc` Compose stack gated BFF on Accounts/Expense Core/Notifications readiness; a real Keycloak client-credentials token produced HTTP 200 `{"data":{"groups":[]}}` through BFF to Expense Core with no connection-refused or upstream-401 log entries; focused BFF compilation and contract validation passed | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Re-ran authenticated Bruno coverage against the rebuilt OIDC stack | Bruno v4.1.0 passed 51/51 requests and 58/58 assertions/tests with the real Keycloak token injected through `local-oidc`; `:app:bff:test --tests '*BffFanoutTest'` and contract validation also passed | current increment |
| 2026-09-20 | AUTH-04 | Removed the remaining direct JWT claim/default/error literals from the reactive decoder factory | `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon`, public-surface validation, and `git diff --check` passed; servlet and reactive decoder factories now share the same OIDC constants | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added malformed-bearer Bruno coverage for all four applications | Against the rebuilt `local-oidc` Compose stack, Accounts, Expense Core, Notifications, and BFF each returned HTTP 401; Bruno passed 4/4 requests and 4/4 tests; public-surface validation reports 50 requests and 55 assertion-backed requests | current increment |
| 2026-09-20 | AUTH-07B | Removed deterministic fallback encryption/digest keys and enabled the protected Accounts auth-email outbox topology in local Compose | Accounts and Notifications now require injected cryptographic environment variables; Accounts receives RabbitMQ settings and `PENNYWISE_AUTH_EMAIL_OUTBOX_ENABLED=true`; Compose config, contract validation, and diff checks passed; broker consumer/Mailpit delivery remains open | security increment |
| 2026-09-20 | AUTH-07B | Completed protected auth-email broker delivery and passwordless runtime smoke journey | Fresh RabbitMQ/Postgres/Keycloak/Mailpit Compose stack: login start returned 202, one encrypted outbox event was consumed by the dedicated auth-email queue, Mailpit received exactly one sign-in-code message, verification returned access and refresh tokens, refresh rotation succeeded, and reuse of the prior refresh token returned 401; no credential was logged by Accounts or Notifications | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added a dedicated Keycloak negative-audience service-account fixture | `pennywise-ci-wrong-audience` has client credentials but no `pennywise-api` audience mapper, enabling a real wrong-audience token journey without token mutation; endpoint execution and CI injection remain the next validation increment | fixture increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Executed the real wrong-audience token journey across every protected boundary | Keycloak minted a signed token from `pennywise-ci-wrong-audience`; Bruno asserted HTTP 401 at Accounts, Expense Core, Notifications, and BFF (4/4 requests, 4/4 tests); CI now injects the negative token through a dedicated output | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added and executed forged-signature HTTP E2E coverage | `tests/e2e/test_oidc_negative.py` mutates the first signature character of a real Keycloak JWT and verified HTTP 401 at Accounts, Expense Core, Notifications, and BFF; Python syntax and workflow YAML validation passed; repository `uvx` mypy remains CI-owned because `uvx` is unavailable in this shell | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Extended signed-token E2E coverage to unsupported algorithms | The same real-token harness rewrites only the JWT header algorithm to `HS256`; all four protected boundaries returned HTTP 401 for both forged-signature and unsupported-algorithm variants | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added forged-token GraphQL WebSocket upgrade coverage | `tests/e2e/test_oidc_websocket_negative.py` mutated a real Keycloak JWT signature and received `HTTP/1.1 401 Unauthorized` from the BFF WebSocket endpoint; the probe is wired into CI | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added a separate `pennywise-other` Keycloak realm and wired its signed service-account token into the negative OIDC harness | Fresh multi-realm Compose stack minted a real token with the wrong issuer; Accounts, Expense Core, Notifications, and BFF all returned HTTP 401, alongside the existing forged-signature and unsupported-algorithm variants; CI now injects `WRONG_ISSUER_TOKEN` | current increment |
| 2026-09-20 | AUTH-04 | Centralized remaining OIDC algorithm-policy and deployment-guard messages in `OidcSecurityConstants` | `./gradlew.bat :libs:security:test --rerun-tasks --no-daemon`, `./gradlew.bat compileKotlin --no-daemon`, contract validation, and `git diff --check` passed; servlet/reactive decoder and configuration guard now share one protocol vocabulary | current increment |
| 2026-09-20 | AUTH-03/AUTH-06 | Reconciled tracker evidence with the verified OIDC boundary matrix | Registry now records passing configuration-guard, WebSocket, malformed-bearer, wrong-audience, wrong-issuer, signature, and algorithm evidence; expiry and invalid-subject remain explicitly open | current increment |
| 2026-09-20 | AUTH-04 | Added provider-neutral temporal JWT regression coverage | `OidcJwtTemporalPolicyTest` proves expired and not-yet-valid claims are rejected and an in-window token is accepted; live provider-issued expiry and invalid-subject journeys remain open | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added assertion-backed wrong-issuer Bruno requests for Accounts, Expense Core, Notifications, and BFF | `validate_public_surface.py` reports 50 REST requests and 63 assertion-backed requests; the local OIDC environment consumes `PENNYWISE_BRUNO_WRONG_ISSUER_TOKEN`, and CI injects the real token minted by the separate Keycloak realm | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Executed the new wrong-issuer Bruno matrix against a fresh multi-realm Compose stack | All four wrong-issuer requests returned HTTP 401 with passing assertions; the focused quality run was 14/15 requests because the pre-existing authenticated `error-attribution` request returned HTTP 401 instead of its expected HTTP 404 and requires a separate authorization-fixture correction | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Fixed Bruno runner injection for negative OIDC tokens | `Makefile` now passes valid, wrong-audience, and wrong-issuer token variables explicitly to Bruno; rerunning authenticated error attribution plus all four wrong-issuer requests passed 5/5, including the expected structured 404 | current increment |
| 2026-09-20 | AUTH-07/AUTH-04 | Added isolated test-only cryptographic fixtures for Accounts and Notifications Spring contexts | The required secret bindings remain fail-closed in deployed profiles while `./gradlew.bat test --no-daemon` now passes all 40 actionable tasks; contract validation and workflow YAML validation also pass | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added same-issuer live expired-token coverage | CI temporarily sets the existing `pennywise` realm lifespan to one second through the Keycloak admin API, mints the normal signed/audienced CI token, restores the 300-second lifespan, and injects `EXPIRED_TOKEN`; the four-boundary probe verifies HTTP 401 without conflating expiry with wrong issuer | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Corrected expiry-fixture administration to use Keycloak `kcadm.sh` inside the provider container | The public admin token path lacked realm-management permissions; CI now uses the bootstrap admin adapter to set/restore the existing `pennywise` realm lifespan, avoiding a false expired-token claim | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Verified same-issuer expiry after bounded clock skew | Fresh Compose run minted a `pennywise` token with `exp = iat + 1`, restored the normal 300-second realm setting, waited beyond the 60-second validator skew, and verified HTTP 401 at Accounts, Expense Core, Notifications, and BFF; CI now waits 65 seconds | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added expired-token Bruno parity across all four protected boundaries | OIDC Bruno environment and Make runner now inject `EXPIRED_TOKEN`; four assertion-backed requests exercise the same expired signed token alongside the Python negative harness | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Executed expired-token Bruno parity against fresh Compose | Same-issuer token expired beyond the 60-second clock-skew window; Accounts, Expense Core, Notifications, and BFF Bruno requests passed 4/4 with HTTP 401 | current increment |
| 2026-09-20 | AUTH-06 | Reconciled the remaining-gates wording after live expiry and wrong-issuer verification | AUTH-06 now lists seeded-user fixture replacement and invalid-subject provider coverage as the remaining authentication gates; previously verified WebSocket, Mailpit, malformed-bearer, wrong-issuer, and expiry journeys are no longer described as open | current increment |
| 2026-09-20 | AUTH-05/AUTH-06 | Made live acceptance fail closed when no signed bearer token is injected | `tests/acceptance/runner.py --require-services` now exits with code 2 instead of using `test-user`; compatibility-only offline/unit harness behavior remains unchanged and documented | current increment |
| 2026-09-20 | AUTH-05/AUTH-06 | Removed the remaining live k6 placeholder bearer fallback | `tests/load/k6/lib/config.js` now requires `BEARER_TOKEN`; k6 and acceptance documentation describe signed-token injection as mandatory | current increment |
| 2026-09-20 | AUTH-05 | Reconciled AUTH-05 and local release checklist wording with the signed-token runtime | Documentation now distinguishes removed runtime passthrough from compatibility-only test fixtures and no longer names `test-user` as a live notification identity | current increment |
| 2026-09-20 | AUTH-06 | Reconciled seeded-user fixture status | Live CI and required-services acceptance use signed service-account tokens; remaining AUTH-06 authentication evidence is limited to provider-issued invalid-subject coverage | current increment |
| 2026-09-20 | AUTH-04 | Centralized the symmetric JWT algorithm-family prefix in the shared OIDC constants | Security policy tests and compilation continue to pass; algorithm policy no longer embeds the protocol prefix in its adapter | current increment |
| 2026-09-20 | AUTH-04/AUTH-06 | Added isolated signed invalid-subject provider coverage | The test-only RSA OIDC issuer is wired through a Compose override; its whitespace-only `sub` was rejected with HTTP 401 by Accounts, Expense Core, Notifications, GraphQL BFF, and the GraphQL WebSocket upgrade, and all four dedicated Bruno requests passed; production Compose remains unchanged | current increment |
| 2026-09-20 | AUTH-05/CI | Fixed mypy failures in live E2E console setup | Replaced unsupported `typing.TextIO.reconfigure()` calls with runtime-safe `io.TextIOWrapper` guards across five E2E scripts; all five modules compile cleanly, while hosted `uvx mypy --config-file mypy.ini tests tools` remains the CI type gate | current increment |
| 2026-09-20 | AUTH-04 | Consolidated the remaining Accounts subject adapter onto shared OIDC policy | `OidcSubjectValidator` now reuses `OidcJwtClaimPolicy` and `OidcSecurityConstants` instead of maintaining a second regex and literal rejection messages; `:libs:security:test :app:accounts:test --rerun-tasks --no-daemon` passed | current increment |
| 2026-09-20 | AUTH-03/AUTH-04 | Centralized the configured JWT algorithm allow-list default | All four application security configurations now delegate empty/default parsing to `OidcSecurityConstants.configuredSigningAlgorithms`; shared security tests and all four application Kotlin compile tasks passed, eliminating duplicated `RS256` defaults | current increment |
| 2026-09-20 | CI/AUTH-05 | Corrected security-hygiene classification of generated CI tokens and explicit E2E placeholders | The scanner now ignores shell-generated values and clearly named test-only identities while retaining literal credential detection; `py -3 tools/ops/check_security_hygiene.py` passes across 674 tracked files | current increment |
| 2026-09-20 | CI | Made the security-hygiene scanner conform to repository typing conventions | Added explicit container and regex types, structured file handling, and typed control flow; hygiene, Python compilation, contract validation, and diff checks pass | current increment |
| 2026-09-20 | AUTH-06 | Removed the last stale task-detail claim that WebSocket and invalid-token evidence remained open | AUTH-06 now reflects the verified Keycloak and isolated test-issuer matrices; contract validation and diff checks pass | current increment |
| 2026-09-20 | AUTH-07 | Reconciled the task detail with the completed runtime slice | Removed pre-implementation claims that controllers, delivery, refresh/logout, and live E2E evidence were deferred; the detail now links the verified implementation and retains only genuinely future secret-rotation policy work | current increment |
| 2026-09-20 | PR-17 | Implemented settlement ledger effects and durable expense idempotency baseline | Settlement and expense JPA focused suites pass; V10/V11 migrations, scoped payload-bound idempotency, replay/conflict handling, and settlement compensating postings are implemented; concurrent PostgreSQL idempotency and reconciliation evidence remain open | current increment |
| 2026-09-20 | PR-18 | Bounded Expense Core persistence reads | Expense and search adapters now apply database-side PageRequest limits and stable creation ordering; Expense Core test compilation and diff checks pass; mutation-time membership revalidation remains open | current increment |
| 2026-09-20 | PR-18 | Added mutation-time membership revalidation | JPA create/update/delete now recheck ACTIVE membership under a pessimistic membership-row lock after locking the group; Kotlin test compilation passes; concurrent removal-versus-mutation evidence remains open | current increment |
| 2026-09-20 | PR-19 | Added durable notification dead-letter routing and production broker fail-closed guard | Notification/auth-email queues declare dead-letter exchanges and operator queues; in-memory broker fallback is excluded from production/staging profiles; notification compilation passes; live retry/rejection evidence remains open | current increment |
| 2026-09-20 | PR-20 | Added GraphQL depth, complexity, and subscription bounds | BFF installs configurable max-depth/max-complexity instrumentation and enforces per-user subscription limits with expiry cleanup; BFF compilation and test compilation pass | current increment |
| 2026-09-20 | PR-21 | Added CI architecture and SBOM baseline gates | Dependency-light architecture and centralized-version validators are wired into reusable CI alongside security hygiene; Python execution is unavailable in this Windows shell, so local script results remain CI-owned | current increment |
| 2026-09-20 | PR-22 | Removed notification persistence fallback | `NotificationInbox` no longer defaults to process-local storage; tests construct the in-memory adapter explicitly, leaving Spring production wiring to the JPA adapter; Notifications test suite passed | current increment |
| 2026-09-20 | PR-23 | Removed additional in-memory Spring persistence adapters | Profile, settlement, and synchronization in-memory adapters are no longer component-scanned; Accounts and Expense Core test suites passed | current increment |
| 2026-09-20 | PR-17 | Serialized expense idempotency claims under the group lock | Expense creation now acquires the pessimistic group lock before idempotency lookup/claim; focused JpaExpenseStoreTest passed; multi-transaction PostgreSQL evidence and reconciliation report remain open | current increment |
| 2026-09-20 | PR-17 | Added assertion-backed settlement reconciliation coverage | `JpaSettlementStoreTest` passed with two balanced participant postings on record, four balanced postings after one reversal, and no additional postings after a repeated reversal; concurrent PostgreSQL idempotency evidence and a standalone reconciliation procedure remain open | current increment |
| 2026-09-20 | PR-24 | Removed Accounts export/deletion service persistence defaults | Spring services now require store injection; in-memory adapters remain available only through explicit clock-based test constructors; Accounts test validation is next | current increment |
| 2026-09-20 | PR-19 | Added auth-email poison-message classification and listener coverage | Malformed auth-email JSON is now classified as non-requeue poison and routed to the DLQ; focused `AuthEmailRabbitListenerTest` and the prior full Notifications suite pass; live RabbitMQ retry/rejection evidence remains open | current increment |
| 2026-09-20 | PR-25 | Restricted internal JWT provider fallback to development profiles | `IdentityProviderPort` fallback is now excluded from `production`, `staging`, and `local-oidc`, forcing explicit provider wiring in deployed profiles; Accounts validation is next | current increment |
| 2026-09-20 | PR-21 | Added real CycloneDX SBOM generation to CI | Centrally versioned CycloneDX Gradle plugin generates aggregate JSON via `cyclonedxBom`; reusable CI uploads the artifact and fails when it is absent; local Gradle execution is the next validation | current increment |
| 2026-09-20 | PR-17 | Added opt-in PostgreSQL concurrent idempotency evidence test | `PostgresExpenseIdempotencyConcurrencyTest` coordinates two real transactions, asserts the second waits on the group lock, and verifies one idempotency row plus one posting set; execution requires `PENNYWISE_POSTGRES_TESTS=true` and Compose PostgreSQL | current increment |
| 2026-09-20 | PR-17 | Executed concurrent idempotency test against local Compose PostgreSQL | With `SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/pennywise_expense_core`, `PENNYWISE_POSTGRES_TESTS=true`, and the Compose credentials, `PostgresExpenseIdempotencyConcurrencyTest` passed; standalone reconciliation report remains open | verified evidence |
| 2026-09-20 | PR-17 | Added operator ledger reconciliation procedure | `docs/operations/ledger-reconciliation.md` defines read-only zero-sum, active-expense coverage, settlement cardinality/sign, and release-evidence checks; execution against the release database remains open | current increment |
| 2026-09-20 | PR-17 | Executed ledger reconciliation queries against local Compose PostgreSQL | Zero non-zero group ledgers, zero uncovered active expenses, and zero invalid settlements; the database had zero settlements, so populated-settlement evidence remains open | verified evidence |
| 2026-09-20 | PR-18 | Corrected Expense Core ORM collection fetching | Expense payer/allocation collections changed from EAGER to LAZY, read materialization is now transactional, and unbounded repository methods were removed; Expense Core validation is next | current increment |
| 2026-09-20 | PR-18 | Added PostgreSQL membership-removal race evidence test | `PostgresMembershipMutationRaceTest` coordinates removal and expense creation on the locked group row and asserts no postings after removal wins; live execution is next | current increment |
| 2026-09-20 | PR-18 | Executed membership-removal race against local Compose PostgreSQL | The removed member subject was supplied through the public authorization path; the waiting mutation failed after removal committed and produced zero balance postings; targeted Gradle test passed | verified evidence |
| 2026-09-20 | PR-19 | Found and documented RabbitMQ topology migration blocker | Rebuilt Notifications against current code; broker rejected redeclaration of the pre-existing source queue because its DLX argument was absent (`PRECONDITION_FAILED`), so DLQs were not declared; added controlled drain/recreate verification procedure and retained live messaging evidence as open | blocker evidence |
| 2026-09-20 | CI | Ran full Gradle regression after production-hardening changes | `./gradlew.bat test --no-daemon` passed all application and library test tasks; live RabbitMQ topology migration remains a separate open gate | verified evidence |
| 2026-09-20 | PR-26 | Added financial participant and request bounds | Locked JPA create/update validates participants against active memberships and duplicate role IDs; request DTOs bound participant/category sizes and idempotency headers; Expense Core suite passed, with dedicated HTTP rejection coverage still open | current increment |
| 2026-09-20 | PR-26 | Added explicit controller request-bound enforcement | Create/update now enforce category, payer/allocation count, and idempotency-key limits even in standalone MVC contexts; `ExpenseControllerTest` passed; dedicated oversized-request assertions remain to be added | current increment |
| 2026-09-21 | PR-26 | Added public HTTP request-bound rejection tests | MockMvc proves oversized idempotency keys, categories, and payer collections are rejected before financial processing; `./gradlew.bat :app:expense-core:test --tests '*ExpenseControllerTest' --no-daemon` passed | verified evidence |
| 2026-09-21 | PR-27 | Hardened notification delivery against fail-open privacy and rate-limit paths | Preference failures and missing preferences suppress email, synthetic recipients are rejected, and `DeliveryRateLimiter` is wired before dispatch; Notifications test suite passed, including explicit rate-limit coverage | verified evidence |
| 2026-09-21 | PR-28 | Added deployed-profile BFF upstream configuration guard | `production` and `staging` profiles now require explicit Accounts and Expense Core URLs; `./gradlew.bat :app:bff:test --tests '*UpstreamConfigurationTest' --no-daemon` passed for missing and valid values | verified evidence |
| 2026-09-21 | PR-29 | Added checked financial arithmetic | Payer, allocation, and settlement aggregation use checked arithmetic; overflow boundary tests passed in the focused Expense Core suite | current increment |
| 2026-09-21 | PR-30 | Revoked removed-member subscriptions from BFF fanout | `member.removed` events now revoke only the removed subject's subscriptions for the affected group before delivery; focused BFF event-consumer coverage added | current increment |
| 2026-09-21 | PR-31 | Added deployed-profile messaging capability guards | Production/staging validators now require Expense Core outbox + Rabbit publishing and Accounts auth-email outbox enablement; focused configuration tests added | current increment |
| 2026-09-21 | PR-32 | Aligned production-like deployment configuration | Staging now supplies consumed BFF URL names, requires infrastructure image references and messaging flags, and inherits explicit `production` profile activation | current increment |
| 2026-09-21 | PR-33 | Classified explicit QA bearer fixtures in security hygiene | Scanner exclusions now require a `security-hygiene: test-fixture` marker under test paths; the previously failing security-hygiene check is being rerun | current increment |
| 2026-09-21 | PR-34 | Redacted notification recipient and exception logs | Email delivery logs now use opaque recipient IDs and classified error types without raw messages; focused redaction coverage added | current increment |
| 2026-09-21 | PR-35 | Restricted actuator access to health probes | Accounts, Expense Core, Notifications, and BFF security chains now permit only `/actuator/health/**`; diagnostic actuator endpoints require authentication | current increment |
| 2026-09-21 | PR-36 | Added versioned notification queue defaults | Notifications source/DLQ defaults now use `.v2` names with DLX declarations; live local RabbitMQ shows both `.v2` consumers and source DLX arguments while legacy queues remain untouched with four messages; operator drain/replay remains required | verified evidence |
| 2026-09-21 | PR-19 | Executed local poison-message DLQ probe on versioned topology | Authenticated local RabbitMQ accepted one malformed event; Notifications rejected it without requeue and `pennywise.notifications.v2.dlq` contained one message; production-like retry/alert evidence remains open | verified evidence |
| 2026-09-21 | PR-37 | Added GraphQL complexity transport evidence | HTTP transport rejects a forty-alias complexity-abuse query before downstream gateways are invoked; `./gradlew.bat :app:bff:test --tests '*GraphqlHttpTransportTest' --no-daemon` passed | verified evidence |
| 2026-09-21 | PR-38 | Normalized CI workflow line endings and release-gate command parity | Yamllint passed for all workflow files; `python tools/ops/validate_release_gate.py` passed with five repository release assets; environment restore/security/capacity/rollback gates remain explicitly open | verified evidence |
| 2026-09-21 | PR-39 | Added production application topology controls | Production Compose now declares overridable three-replica and CPU/memory limit/reservation defaults for stateless services; render validation is pending | current increment |
| 2026-09-21 | PR-40 | Added bounded idempotency retention cleanup | Expense idempotency claims now retain for 24 hours by default and are deleted in scheduled batches of at most 500; focused cleanup test added | current increment |
| 2026-09-21 | PR-41 | Made BFF financial fanout fail closed | Removed empty-list fallbacks for balance and expense upstream failures; balance 503 and expense 502 propagation tests were added; `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon` passed | verified evidence |
| 2026-09-21 | PR-42 | Bounded RabbitMQ transient redelivery | Notification and auth-email listeners now requeue only first transient delivery and reject redeliveries to the DLQ; full `./gradlew.bat :app:notifications:test --rerun-tasks --no-daemon` passed with both listener redelivery tests; production-like retry timing and alert evidence remain open | verified evidence |
| 2026-09-21 | PR-43 | Wired configurable GraphQL subscription bounds | BFF GraphQL properties now control per-user subscription count, queue capacity, and TTL in the production fanout bean; constructor validation rejects invalid bounds; `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon` passed | verified evidence |
| 2026-09-21 | PR-44 | Serialized GraphQL subscription admission | Expiry cleanup, per-user limit check, and subscription insertion are now atomic; concurrent eight-attempt coverage admits exactly one at a limit of one; full BFF test suite passed | verified evidence |
| 2026-09-21 | PR-45 | Locked recurring worker due-schedule claims | Due recurring schedules now use a database pessimistic write lock held through the transactional worker batch, preventing concurrent replica processing; full Expense Core suite passed | verified evidence |
| 2026-09-21 | PR-46 | Added durable settlement recording idempotency | Settlement recording now requires a bounded Idempotency-Key and uses a stable group/actor/key-derived identity; replay conflicts are rejected; OpenAPI and contract validation passed; focused and full Expense Core test suites passed with altered-payload replay coverage; production PostgreSQL concurrency evidence remains open | verified evidence |
| 2026-09-21 | PR-17 | Revalidated PostgreSQL idempotency and ledger reconciliation | Opt-in `PostgresExpenseIdempotencyConcurrencyTest` passed against local Compose PostgreSQL; read-only reconciliation returned zero non-zero ledgers, zero uncovered active expenses, zero invalid settlements, and zero settlements in the database, so populated-settlement evidence remains open | verified evidence |
| 2026-09-21 | PR-47 | Added populated PostgreSQL settlement reconciliation evidence | Opt-in `PostgresSettlementReconciliationTest` created recorded and reversed settlements through JPA and asserted 2/4 postings, zero-sum totals, and directional signs; passed against local Compose PostgreSQL | verified evidence |
| 2026-09-21 | CI | Ran full application regression after settlement hardening | `./gradlew.bat :app:accounts:test :app:expense-core:test :app:notifications:test :app:bff:test --rerun-tasks --no-daemon` passed all four application suites | verified evidence |
| 2026-09-21 | PR-46 | Added public settlement replay/conflict evidence | `SettlementControllerTest` passed with same-key HTTP replay returning the original settlement and altered payload returning 409 Conflict | verified evidence |
| 2026-09-21 | PR-21 | Re-ran CI, security, SBOM, architecture, and release gates | `check_security_hygiene.py`, `check_architecture.py`, `validate_sbom_baseline.py`, and `validate_release_gate.py` all passed; restore, security scan, capacity, and rollback environment gates remain open | verified evidence |
| 2026-09-21 | PR-48 | Added pull-request dependency vulnerability review | Reusable CI now runs `actions/dependency-review-action@v4` for pull requests with `fail-on-severity: high`; workflow yamllint, registry validation, and diff checks passed | verified evidence |
| 2026-09-21 | PR-49 | Added bounded GraphQL transport request sizes | BFF now limits codec buffering to 256 KB and request headers to 16 KB by configurable deployment properties; full `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon` passed | verified evidence |
| 2026-09-21 | CI | Revalidated contracts and static production gates after PR-49 | Contract validation, security hygiene, architecture boundaries, SBOM baseline, and workflow yamllint all passed; external vulnerability scanning and environment gates remain separate | verified evidence |
| 2026-09-21 | CI / PR-48 | Fixed reusable dependency-review permission propagation | Added read-only `pull-requests: read` to PR and branch callers; pinned yamllint passed for `.github/workflows` and `git diff --check` passed; hosted GitHub validation remains the final confirmation | current increment |
| 2026-09-21 | CI | Fixed SBOM lint-job toolchain setup | The reusable lint job now installs Microsoft JDK 25 and Gradle setup before `cyclonedxBom`; workflow yamllint and diff checks remain required | current increment |
| 2026-09-21 | PR-19 | Fixed premature outbox publication during broker outage | Rabbit publisher now waits for publisher confirms before marking events `PUBLISHED`; focused messaging tests passed and chaos E2E verified `PENDING` during RabbitMQ pause, recovery drain, and Notifications delivery | current increment |
| 2026-09-21 | DB-01..DB-17 | Reworked CQRS task decomposition from broad strategy into code-targeted implementation tasks | Added `docs/implementation/cqrs-code-inventory.md`, 17 linked task specifications with concrete application/library/configuration/test paths, registry and board entries; `py -3 tools/contracts/validate.py` validated 197 tasks and `git diff --check` passed | planned workstream |
| 2026-09-21 | DB-01/DB-03/DB-04/DB-13 | Started CQRS implementation foundation | Added initial `libs/db` route/operation/consistency types and writer-safety guard; wired Accounts, Expense Core, and Notifications to `libs:db`; added optional PostgreSQL 17 streaming-replica Compose override with WAL, replication role, pg_hba, and base-backup entrypoint. `:libs:db:test`, all three service Kotlin compile tasks, default Compose config, merged replica Compose config, and `git diff --check` passed. Live replication smoke and datasource auto-configuration remain open. | current increment |
| 2026-09-21 | DB-04/DB-13 | Added opt-in datasource pool configuration and verified live local streaming replication | `libs/db` now validates writer/named-reader Hikari settings and only activates when `pennywise.db.enabled=true`; focused library tests and three service compiles passed. Docker primary reports `pennywise_replica` in `streaming` state and replica reports `pg_is_in_recovery() = true` with matching LSNs. Existing-volume replication role repair was required; documented. | verified evidence |
| 2026-09-21 | DB-03 | Wired datasource route selection to scoped execution context | `DbRoutingDataSource` now defaults to writer and selects a reader only for an explicitly reader-eligible non-strong query context; ThreadLocal context restoration and writer-safety tests pass. Application repositories are not yet annotated/classified, so no production query is reader-routed yet. | current increment |
| 2026-09-21 | DB-07 | Routed Expense Core search/export query slice to optional local reader | Membership authorization remains before the query on the writer; `SearchController` scopes only `expense.search` and `expense.search.export` as eventual reader-eligible contexts. Expense Core application properties and replica Compose override supply writer/reader endpoints. `:app:expense-core:test --tests '*Search*'`, full dev+replica Compose config, contract validation, and diff checks passed; SQL-side filtering/keyset pagination remains open. | current increment |
| 2026-09-21 | DB-07 | Replaced Expense Core search preload with bounded SQL projection and UUID keyset query | `ExpenseRepository.searchProjection` applies group/deleted/text/currency/category/cursor filters in SQL and uses `Pageable` for limit-plus-one; `JpaSearchStore` maps scalar projections; in-memory and JPA tests align on UUID cursor ordering. `JpaSearchStoreTest` passed; live reader-route E2E remains open. | current increment |
| 2026-09-21 | DB-08/DB-09 | Added explicit reader policies for Accounts profile lookup and Notifications historical inbox listing | Accounts profile-by-ID/batch and Notifications historical inbox page now use reader-eligible contexts; `/me` provisioning, profile mutation, notification mutation, deduplication, preferences, and delivery state remain writer-bound. Service pool configuration and replica Compose endpoints added; Expense Core search suite passed, full Accounts/Notifications rerun is next. | current increment |
| 2026-09-21 | E2E / DB-07..DB-09 | Stabilized replica-enabled live E2E coverage | Corrected all affected E2E financial fixtures to use group membership IDs, added the required settlement idempotency header in the BFF and REST edge probe, and ran fresh signed-persona suites against the live replica topology. Product lifecycle, offline replay, WebSocket/concurrency, chaos/outbox recovery, REST edge cases, acceptance health/auth flows, contract validation, and service tests passed; the initial acceptance service-account run was correctly rejected until the internal-issuer token was minted with `--connect-to`. | verified evidence |
| 2026-09-21 | DB-02/DB-05 | Added CQRS consistency contract and reader circuit policy | Added `docs/architecture/cqrs-data-access.md` and `DbReaderHealth` state/circuit decisions. Eventual queries may use bounded writer fallback after reader failure; strong/writer-only operations remain writer-routed. `:libs:db:test` and the full Accounts/Expense Core/Notifications/BFF regression passed; replay-LSN probes, scheduled lag measurement, metrics, and operational alert evidence remain open. | current increment |
| 2026-09-21 | DB-05 | Added PostgreSQL replay-lag probe and bounded scheduler | `DbReaderLagProbe` reads `pg_last_xact_replay_timestamp()` from named reader pools only; `DbReaderHealthScheduler` marks lagging/disconnected readers without probing or consuming writer connections. Replica-enabled Accounts, Expense Core, and Notifications booted healthy and started their named reader pools; `:libs:db:test` passed. Metrics and alert rules remain open. | current increment |
| 2026-09-21 | DB-11 | Added low-cardinality route and reader-health telemetry | `DbTelemetry` records operation route, bounded fallback, reader failure, and replay lag without SQL parameters or payloads; routing and scheduled probes share the component. `:libs:db:test :libs:observability:test` passed. Duration/pool/lock metrics and dashboards remain open. | current increment |
| 2026-09-21 | DB-10 | Added causal PostgreSQL LSN watermark kernel | `DbWatermark` parses/serializes PostgreSQL LSNs; reader health tracks replayed positions and routes below-watermark reads to explicit writer fallback or writer-only execution. DB library tests passed; HTTP/service/BFF watermark propagation remains open. | current increment |
| 2026-09-21 | DB-10 | Added service and BFF causal-header propagation | Stateful services now validate inbound required LSNs and emit writer LSN headers after successful mutations; BFF WebFlux context captures and forwards required watermarks through downstream gateways. `:libs:db:test` and BFF compilation passed; response capture and end-to-end causal proof remain open. | current increment |
| 2026-09-21 | DB-10 | Preserved downstream writer watermarks through BFF responses | BFF WebClient response filters now select the greatest valid downstream LSN and copy it to the inbound response; BFF test suite passed. Full end-to-end command/query causal proof remains open. | current increment |
| 2026-09-21 | DB-12 | Added local replica performance smoke evidence | `tests/performance/cqrs-replica-smoke.sh` passed with merged Compose: `replica_streaming=t`, `replica_in_recovery=t`, replay LSN `0/40B24D8`, and authenticated route telemetry present. This remains local topology evidence; capacity, contention, and failure-budget evidence remain open. | current increment |
| 2026-09-21 | DB-11/DB-16 | Added CQRS reader alert rules and operator runbook | Prometheus now alerts on replay lag, reader failures, and sustained writer fallback; `docs/operations/cqrs-replica.md` documents lag, outage, forced writer-only mode, and evidence boundaries. PyYAML validation, contract validation, and diff checks passed; production alert-routing proof remains open. | current increment |
| 2026-09-21 | DB-06 | Split Expense Core expense command/query ports | Added `ExpenseCommandStore` and `ExpenseQueryStore` with a compatibility `ExpenseStore` facade, preserving existing JPA/in-memory implementations and writer-bound financial behavior. `./gradlew.bat :app:expense-core:test --rerun-tasks --no-daemon` passed; remaining aggregate port splits and route assertions remain open. | verified evidence |
| 2026-09-22 | DB-06 | Split Expense Core group command/query ports | Added `GroupCommandStore` and `GroupQueryStore` with a compatibility `GroupStore` facade. The full Expense Core test suite passed again; settlement, sync, recurring, audit, outbox, and route assertion work remains open. | verified evidence |
| 2026-09-22 | DB-06 | Split settlement and synchronization ports | Added writer-only `SettlementCommandStore` and separate synchronization command/query ports with compatibility facades. Full Expense Core tests passed; recurring, audit, outbox, and route assertion work remains open. | verified evidence |
| 2026-09-22 | DB-06 | Split transactional outbox command/query ports | Added writer-side `OutboxCommandStore` and read-only `OutboxQueryStore` with the existing `OutboxStore` facade; claim and status transitions remain writer-owned. Full Expense Core tests passed; recurring, audit, and route assertion work remains open. | verified evidence |
| 2026-09-22 | DB-06 | Split recurring schedule command/query ports | Added `RecurringCommandStore` and `RecurringQueryStore` implemented by the existing service; due processing and schedule mutations remain writer-bound while reads stay bounded. Full Expense Core tests passed; audit and route assertion work remains open. | verified evidence |
| 2026-09-22 | DB-07 | Added direct search route-policy assertion | `SearchControllerTest` records the adapter execution context and proves authorized `expense.search` uses `QUERY` + `EVENTUAL` + reader eligibility after membership authorization. `./gradlew.bat :app:expense-core:test --tests '*SearchControllerTest' --rerun-tasks --no-daemon` passed; PostgreSQL plan/load and live rollback evidence remain open. | verified evidence |
| 2026-09-22 | DB-08 | Added Accounts profile route-policy assertion | `ProfileControllerTest` records the adapter context and proves `profile.lookup` uses `QUERY` + `EVENTUAL` + reader eligibility while `/me` remains writer-default. `./gradlew.bat :app:accounts:test --tests '*ProfileControllerTest' --rerun-tasks --no-daemon` passed; replica outage and read-after-write evidence remain open. | verified evidence |
| 2026-09-22 | DB-09 | Added Notifications inbox route-policy assertion | `InboxControllerTest` records the adapter context and proves `notification.inbox.history` uses `QUERY` + `EVENTUAL` + reader eligibility while append, mark-read, deduplication, preferences, and delivery state remain writer-bound. `./gradlew.bat :app:notifications:test --tests '*InboxControllerTest' --rerun-tasks --no-daemon` passed; replica outage and subject-isolation integration evidence remain open. | verified evidence |
| 2026-09-22 | DB-12/DB-13/DB-14 | Revalidated local replica pilot and full application regression | `sh tests/performance/cqrs-replica-smoke.sh` passed with `replica_streaming=t`, `replica_in_recovery=t`, and replay LSN `0/40B24D8`; merged Compose config, contract validation, `git diff --check`, and `./gradlew.bat :app:accounts:test :app:expense-core:test :app:notifications:test :app:bff:test --rerun-tasks --no-daemon` all passed. This proves local topology and application compatibility only; failover/promotion, capacity/SLO, and production alert-routing evidence remain open. | verified evidence |
| 2026-09-22 | DB-05/DB-14 | Exercised local reader disconnect and recovery | Stopped only `postgres-replica`; primary `pg_isready -U pennywise -d postgres` remained accepting connections. `docker compose ... up -d --wait postgres-replica` restored the replica to healthy streaming state, and `sh tests/performance/cqrs-replica-smoke.sh` passed again. This is local disconnect/recovery evidence, not promotion or production failover proof. | verified evidence |
| 2026-09-22 | E2E / Windows | Stabilized shell acceptance entrypoint line endings | Added `.gitattributes` with `*.sh text eol=lf`; `bash tests/acceptance/run.sh --no-edge-cases` now executes on the Windows worktree and reaches contract/health checks instead of failing on CRLF parsing. The run then correctly stopped at authenticated QA scenarios because no signed `BEARER_TOKEN` was supplied; live authenticated E2E evidence remains separate. | verified evidence |
| 2026-09-22 | DB-10 | Hardened causal watermark response emission | Removed the premature `!response.isCommitted` gate from `DbCausalWatermarkFilter`; successful non-GET responses now attempt writer-LSN emission after downstream processing, while committed responses safely ignore late headers. `./gradlew.bat :libs:db:test --rerun-tasks --no-daemon` passed; live multi-service causal proof remains open. | verified evidence |
| 2026-09-22 | DB-10 | Added direct causal servlet-boundary regression test | `DbCausalWatermarkFilterTest` proves required LSN scope, writer LSN response emission, and ThreadLocal restoration using mocked JDBC/servlet boundaries. `./gradlew.bat :libs:db:test --rerun-tasks --no-daemon` passed; live multi-service causal proof remains open. | verified evidence |
| 2026-09-22 | CQRS release audit | Re-ran repository-wide verification | `./gradlew.bat test --rerun-tasks --no-daemon` passed all 52 actionable tasks; architecture validation, security hygiene, contract/task validation, and `git diff --check` also passed. Production-like promotion, capacity/SLO, alert routing, and live authenticated causal E2E gates remain open. | verified evidence |
| 2026-09-22 | E2E / DB-07..DB-10 | Stabilized authenticated acceptance against the replica-enabled stack | Fixed the acceptance fixture to resolve real membership IDs and create an active placeholder, added the required settlement `Idempotency-Key`, made BFF placeholder-member subjects nullable, and aligned GraphQL `Member.subject` with that contract. Fresh internal-issuer token run passed QA-04 contracts, health, group/expense/settlement, offline replay, WebSocket query, rollback, concurrency, authorization, BFF fanout, and recovery scenarios. | verified evidence |
| 2026-09-22 | E2E / DB-07..DB-10 | Re-ran full product lifecycle with distinct signed personas | Minted repository-defined internal-issuer tokens for Alice, Bob, and nonmember clients using Keycloak `--connect-to`; `py -3 tests/e2e/test_product_journey.py` passed all 11 lifecycle steps, including membership isolation, GraphQL mutations, idempotency replay, zero-sum balances, settlement, Notifications delivery, and offline sync against the replica-enabled stack. | verified evidence |
| 2026-09-22 | DB-02 / tracking | Reconciled CQRS architecture task status | Marked DB-02 done and synchronized the board with registry evidence; DB-01 is done, DB-03/04/07/08/09/13/14 are in progress, and operational/production gates remain explicitly open. | verified evidence |
| 2026-09-22 | DB-12 | Added repeatable local capacity-smoke evidence | `REQUESTS=100 CONCURRENCY=10 sh tests/performance/capacity-smoke.sh` completed 100/100 BFF readiness requests with zero failures in 2 seconds (50 requests/s). This is local readiness baseline only; authenticated operation mix, p95/p99 budgets, contention, pool exhaustion, and production capacity evidence remain open. | verified evidence |
| 2026-09-22 | DB-11 | Added low-cardinality pool-acquisition timing | `DbTelemetry` now records acquisition count/total and Micrometer operation/pool timers; `DbRoutingDataSource` instruments writer and reader acquisition without SQL or payload labels. `./gradlew.bat :libs:observability:test :libs:db:test --rerun-tasks --no-daemon` passed; lock/deadlock metrics, dashboards, and production query-plan governance remain open. | verified evidence |
| 2026-09-22 | DB-11 | Added lock-wait and deadlock telemetry primitives | `DbTelemetry` now exposes low-cardinality lock-wait/deadlock counters and diagnostic snapshots; observability and DB suites passed. Wiring every store/query path, dashboards, and production query-plan governance remain open. | verified evidence |
| 2026-09-22 | DB-11 | Added CQRS database dashboard panels | Grafana overview now includes reader lag, writer fallback rate, pool-acquisition p95, and lock/deadlock panels with low-cardinality labels. JSON dashboard validation and `git diff --check` passed; production dashboard/alert-routing proof remains open. | verified evidence |
| 2026-09-22 | DB-07/DB-11 | Added repeatable Expense Core search plan check | `sh tests/performance/expense-search-plan.sh` passed against the local replica, requiring `Index Scan using expenses_group_idx`; probe output reported 4 shared-buffer hits and 0.068 ms execution. This is local empty-group plan evidence; populated workload, baseline comparison, and production capacity remain open. | verified evidence |
| 2026-09-22 | DB-07 | Re-ran search plan against a populated lifecycle group | `GROUP_ID=01a0c619-f842-7f95-9406-354abd9d2e47 sh tests/performance/expense-search-plan.sh` retained the `expenses_group_idx` scan, examined one filtered row, and completed in 0.052 ms with 5 shared-buffer hits. No `dinner` row matched; representative result-volume/load evidence remains open. | verified evidence |
| 2026-09-22 | DB-16 | Added repeatable safe replica disconnect/recovery drill | `sh tests/performance/replica-disconnect-recovery.sh` stopped only `postgres-replica`, verified primary `pg_isready` acceptance, restarted with Compose health waiting, and verified `replica_recovery_streaming=t` and `replica_recovery_mode=t`. It never promotes or removes volumes; production promotion/restore gates remain open. | verified evidence |
| 2026-09-22 | DB-16/DB-17 | Re-ran repository release and architecture gates | Release assets, SBOM baseline, observability YAML, architecture boundaries, security hygiene, contracts/task registry, and diff checks passed. The release validator explicitly keeps restore, external security scan, capacity, and rollback-drill environment gates open; no production-readiness claim is made. | verified evidence |
| 2026-09-22 | E2E / DB-05..DB-14 | Re-ran remaining authenticated replica-enabled E2E suites | With distinct signed Alice/Bob/nonmember personas, REST edge cases, offline replay/idempotency, concurrency/WebSocket subscriptions, and chaos/outbox recovery all passed. Chaos confirmed financial ledger isolation and PENDING-to-PUBLISHED outbox recovery after RabbitMQ pause; local E2E evidence remains separate from production failover/capacity proof. | verified evidence |
| 2026-09-22 | DB-03 | Closed explicit datasource route-policy bypass | `DbRoutingDataSource.connection` now validates explicit routes through `DbRouteGuard`; regression tests reject reader acquisition for commands/strong queries and allow approved eventual queries. `./gradlew.bat :libs:db:test --rerun-tasks --no-daemon` passed. | verified evidence |
| 2026-09-22 | DB-06 | Added narrow writer-side group audit port | `GroupAuditCommandStore` now isolates audit persistence from `JpaGroupStore`; `GroupAuditRepository` remains the adapter and preserves existing transaction/test behavior. Expense Core validation is next. | current increment |
| 2026-09-22 | DB-08/DB-09 | Split Accounts profile and Notifications inbox ports | Added explicit command/query interfaces with compatibility facades; profile mutations and inbox mutations remain writer-side while approved historical reads retain their existing route contexts. Accounts and Notifications test suites passed with `--rerun-tasks`. | verified evidence |
| 2026-09-22 | DB-11 | Added low-cardinality query-duration governance API | `DbTelemetry.queryDuration` tracks query totals and slow-query classifications with operation/route labels only; observability and DB suites passed with `--rerun-tasks`. Store-level invocation and production baseline evidence remain open. | verified evidence |
| 2026-09-22 | DB-07/DB-11 | Wired query-duration telemetry into Expense Core search | Bounded search and CSV export now measure query blocks with stable operation names; `*SearchControllerTest` and observability tests passed with `--rerun-tasks`. | verified evidence |
