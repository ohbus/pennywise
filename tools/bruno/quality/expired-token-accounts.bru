meta {
  name: Expired token is rejected by Accounts
  type: http
  seq: 15
}

get {
  url: {{baseUrlAccounts}}/accounts/v1/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{expiredToken}}
}

tests {
  test("expired token is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
