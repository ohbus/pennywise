# DB-14 — Execute one controlled replica pilot

Enable exactly one non-financial historical query against a replica. Verify
staleness budgets, causal routing, circuit breaking, writer fallback, promotion
simulation, reader disconnect, and writer capacity protection.

This is a HITL release gate. Acceptance requires reviewed load/failure evidence,
approved SLOs, rollback switch verification, and explicit go/no-go recording.

## Concrete code targets

- Enable the reader route only for the DB-07 Expense Core search operation and its named configuration property.
- Use the DB-13 replica topology and `tests/performance/`/`tests/acceptance/` harness; do not broaden application routing in this task.
- Update `docs/operations/compose-topology.md`, replica runbooks, release checklist, and task progress with observed lag, fallback, and recovery results.
