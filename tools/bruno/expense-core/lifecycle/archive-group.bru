meta {
  name: Archive Group
  type: http
  seq: 1
}

post {
  url: {{baseUrlExpenseCore}}/expense-core/v1/groups/{{groupId}}/archive
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("group archive succeeds", function() {
    expect(res.getStatus()).to.eql(200);
    expect(res.getBody().status).to.eql("ARCHIVED");
  });
}
