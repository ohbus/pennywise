meta {
  name: List Expenses
  type: http
  seq: 2
}

get {
  url: {{baseUrlExpenseCore}}/expense-core/v1/groups/{{groupId}}/expenses
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("expenses are listed", function() {
    expect(res.getStatus()).to.eql(200);
    expect(res.getBody()).to.be.an("array");
  });
}

query {
  cursor:
  category:
}
