meta {
  name: Wrong issuer is rejected by BFF
  type: http
  seq: 14
}

post {
  url: {{baseUrlBff}}/graphql
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{wrongIssuerToken}}
}

body:json {
  {
    "query": "{ groups { id name } }"
  }
}

tests {
  test("wrong issuer is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
