meta {
  name: Request Data Export
  type: http
  seq: 4
}

post {
  url: {{baseUrlAccounts}}/accounts/v1/me/export-request
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

tests {
  test("export request is accepted", function() {
    expect(res.getStatus()).to.eql(202);
    expect(res.getBody().exportId).to.be.ok;
  });
}
