# PR-23 — Profile and financial adapter production wiring

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-22, FND-08, AUTH-03
- Owned paths: Accounts and Expense Core in-memory adapter sources, focused context tests, and this task detail

## Objective

Prevent process-local profile, settlement, and synchronization adapters from
being component-scanned alongside durable JPA adapters in deployed profiles.

## Acceptance criteria

- In-memory adapters are explicit test utilities, not Spring production beans.
- JPA adapters remain the unambiguous production implementations.
- Application context tests prove durable adapter selection.

## Validation

- `./gradlew.bat :app:accounts:test :app:expense-core:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Removed Spring component scanning from the in-memory profile, settlement, and
synchronization adapters. Durable JPA adapters remain the application beans;
in-memory implementations are explicit test utilities. Accounts and Expense
Core test suites pass.
