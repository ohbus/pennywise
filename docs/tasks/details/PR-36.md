# PR-36 — Versioned notification queue topology

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-19, PR-32
- Owned paths: Notifications queue properties/listeners, migration operations docs, and this task detail

## Objective

Allow a new Notifications deployment to declare DLX-compatible queues without
redeclaring incompatible legacy queues, while preserving an explicit operator
drain/replay migration path.

## Acceptance criteria

- Default source and DLQ queue names are versioned and have DLX-compatible
  declarations.
- Listener defaults use the same versioned names.
- Existing queue names remain overrideable for controlled migration.
- Migration documentation describes legacy drain, replay, and verification.

## Validation

- `./gradlew.bat :app:notifications:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Versioned `.v2` defaults now avoid incompatible redeclaration of legacy source
queues while retaining property overrides for controlled migration. Listener
defaults, DLQ names, and the migration runbook are synchronized.
