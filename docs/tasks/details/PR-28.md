# PR-28 — BFF fail-closed upstream configuration

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-21, BFF-11
- Owned paths: BFF upstream transport/configuration, focused tests, and this task detail

## Objective

Ensure deployed BFF instances cannot silently use localhost defaults for
Accounts or Expense Core downstream calls.

## Acceptance criteria

- Accounts and Expense Core base URLs are required configuration in deployed
  profiles.
- Local development retains explicit documented defaults or profile-only values.
- Startup/configuration tests prove missing deployed URLs fail closed.
- Upstream failures are not converted to successful empty balances or expenses.

## Validation

- `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Added a `production`/`staging`-profile configuration validator requiring both
downstream URLs. Local gateway defaults remain unchanged for local development;
focused validator tests prove missing deployed addresses fail closed and
explicit addresses are accepted.
