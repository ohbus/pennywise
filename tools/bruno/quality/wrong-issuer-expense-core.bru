meta {
  name: Wrong issuer is rejected by Expense Core
  type: http
  seq: 12
}

get {
  url: {{baseUrlExpenseCore}}/expense-core/v1/groups
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{wrongIssuerToken}}
}

tests {
  test("wrong issuer is rejected", function() {
    expect(res.getStatus()).to.eql(401);
  });
}
