# DB-09 — Split Notifications command and query ports

Implementation is complete: inbox command/query ports, writer-only delivery
state, bounded historical reads, route/telemetry tests, subject isolation, and
authenticated replica E2E are verified. Production SLO/failover gates remain
with DB-14/DB-16.

Keep inbox deduplication, delivery preferences used for sending, leases,
attempts, and operator/DLQ state on the writer. Pilot explicit query routing
for bounded historical inbox reads only.

Acceptance covers stale history behavior, subject isolation, delivery privacy,
reader outage, route metrics, and the complete Notifications test suite.

## Concrete code targets

- Split `inbox/JpaNotificationInboxStore.kt` into historical read DTO queries versus mark-read and subject/authorization operations.
- Keep `NotificationInboxRepository.kt`, `preferences/JpaPreferenceStore.kt`, and `NotificationPreferenceRepository.kt` writer-bound when state affects delivery or mutation.
- Keep `TransactionalNotificationEventProcessor.kt`, `ProcessedNotificationEventRepository.kt`, `JpaEventDeduplicator.kt`, and listener retry/claim paths on the writer.
- Update `InboxController.kt`, `PreferenceController.kt`, `JpaNotificationInboxStoreTest`, `JpaPreferenceStoreTest`, and consumer tests with route assertions.
- Add a bounded historical-inbox query test proving replica outage and stale behavior do not affect delivery decisions.

## Current implementation increment

`NotificationInbox.page` scopes historical inbox listing as an eventual,
reader-eligible query. Mark-as-read, append, deduplication, delivery
preferences, and consumer state remain writer-bound. Notifications now has a
named inbox reader configuration; live reader-route evidence remains open.

## Route evidence (2026-09-22)

`InboxControllerTest` now records the inbox adapter context and asserts
historical listing uses `notification.inbox.history` as a `QUERY` with
`EVENTUAL` consistency and explicit reader eligibility. Append, mark-read,
deduplication, preference, and delivery state paths remain outside this
context and therefore writer-bound. Replica outage and subject-isolation
integration evidence remain open.

`NotificationInboxQueryStore` and `NotificationInboxCommandStore` now
separate historical inbox listing from append/mark-read mutations; the
existing `NotificationInboxStore` facade preserves current adapters.
