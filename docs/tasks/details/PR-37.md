# PR-37 — GraphQL abuse-control transport evidence

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-20
- Owned paths: BFF GraphQL transport tests/configuration, and this task detail

## Objective

Prove GraphQL complexity/depth controls reject abusive operations before
resolver or downstream gateway execution.

## Acceptance criteria

- A complexity-over-limit HTTP operation returns a GraphQL error.
- The rejected operation does not call downstream gateways.
- Existing valid GraphQL transport operations continue to pass.
- Configured bounds remain explicit and testable.

## Validation

- `./gradlew.bat :app:bff:test --tests '*GraphqlHttpTransportTest' --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Added an HTTP transport test issuing forty aliased group selections. The
configured complexity instrumentation rejects the operation before either
downstream gateway is invoked; existing transport tests remain in the same
target.
