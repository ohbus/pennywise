meta {
  name: List Notification Inbox
  type: http
  seq: 1
}

get {
  url: {{baseUrlNotifications}}/notifications/v1/inbox?limit=50
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Accept: application/json
}

tests {
  test("seeded notification inbox is returned", function() {
    expect(res.getStatus()).to.eql(200);
    expect(res.getBody().items).to.be.an("array");
  });
}
