# PR-43 — Wire configurable GraphQL subscription bounds

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-20, PR-37
- Owned paths: BFF GraphQL abuse configuration/application wiring/tests, and this task detail

## Objective

Make the declared GraphQL subscription, queue, and TTL limits effective in the
production `LiveUpdateFanout` bean rather than leaving it on hard-coded defaults.

## Acceptance criteria

- Subscription count, queue capacity, and TTL are configurable under the BFF
  GraphQL properties namespace.
- The application bean receives those values and rejects invalid bounds.
- Existing subscription expiry and limit tests remain passing.

## Validation

- `./gradlew.bat :app:bff:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Added `subscriptionQueueCapacity` and `subscriptionTtlSeconds` to the BFF
GraphQL properties and wired all three subscription bounds into the production
`LiveUpdateFanout` bean. Its constructor rejects non-positive queue capacity,
TTL, and per-user limits. The full BFF test suite passed.
