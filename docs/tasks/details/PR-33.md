# PR-33 — Security hygiene fixture classification

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-21
- Owned paths: security hygiene scanner, explicitly marked test fixtures, focused validation, and this task detail

## Objective

Make the security-hygiene gate pass safe test-only bearer fixtures while
continuing to reject unclassified credential-like material.

## Acceptance criteria

- Test fixtures require an explicit marker to be excluded from the scanner.
- The marker is honored only under test paths.
- Production/source/configuration credential patterns remain findings.
- Scanner and Python type-check validation pass.

## Validation

- `python tools/ops/check_security_hygiene.py`
- `uvx --from mypy==1.17.1 mypy --config-file mypy.ini tests tools`
- `git diff --check`

## Current increment

The scanner now accepts only an explicit `security-hygiene: test-fixture`
marker on test-path lines. The two QA-05 bearer fixtures carry that marker;
production/source/configuration findings remain unclassified and rejected.
