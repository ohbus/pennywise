# PR-21 — CI, security, SBOM, and architecture gates

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: FND-03, OPS-20, DOC-20
- Owned paths: `tools/ops/`, `Makefile`, `.github/workflows/`, `docs/operations/ci.md`, and this task detail

## Objective

Make CI reject known architectural boundary violations, uncentralized
dependency versions, and tracked secret material while retaining a dependency-
light local execution path.

## Acceptance criteria

- Architecture validation rejects cross-service application imports and known
  forbidden infrastructure-to-domain directions.
- SBOM baseline validation confirms the centralized version catalog exists and
  build scripts do not introduce dependency versions outside it.
- Security hygiene remains an executable gate.
- Pull-request and main CI invoke all three checks.

## Validation

- `python3 tools/ops/check_architecture.py`
- `python3 tools/ops/validate_sbom_baseline.py`
- `make security-hygiene`
- `make workflow-validate`
- `git diff --check`

## Current increment

Added typed, dependency-light architecture and centralized dependency-version
baseline validators and wired them into the reusable CI lint job beside the
existing security-hygiene check. The CycloneDX Gradle plugin is now centrally
versioned and applied to all subprojects; CI generates and uploads the
CycloneDX JSON artifact. The local shell has no Python executable; CI execution
remains required evidence for the Python gates.
