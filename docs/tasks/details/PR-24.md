# PR-24 — Remove Accounts request-service in-memory defaults

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-23, FND-08
- Owned paths: Accounts export and deletion request services/stores, focused Accounts tests, and this task detail

## Objective

Remove constructor defaults that silently select process-local export or deletion
request persistence in deployed Accounts contexts.

## Acceptance criteria

- Spring services require an injected `ExportRequestStore` or
  `DeletionRequestStore`.
- In-memory adapters remain available only through explicit test construction.
- Accounts tests prove the explicit test adapters still behave correctly.

## Validation

- `./gradlew.bat :app:accounts:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Registered after the production-readiness review found constructor defaults that
could mask missing durable adapters. No implementation claim is made yet.

The services now require store injection; the clock constructors remain explicit
test conveniences and construct the in-memory adapters only when called directly
by tests. The full Accounts test suite passes, including Spring context startup
with durable JPA request stores.
