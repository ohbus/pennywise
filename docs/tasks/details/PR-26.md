# PR-26 — Expense participant and request-bound validation

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-18, PR-17, CORE-22
- Owned paths: Expense Core expense domain/controller/persistence validation, focused tests, and this task detail

## Objective

Reject financial mutations that reference non-members or exceed bounded input
limits before ledger side effects are written.

## Acceptance criteria

- Every payer and allocation participant is an active group member or active
  placeholder in the locked mutation transaction.
- Duplicate participant IDs and invalid payer/allocation totals are rejected.
- Shared request limits bound participant counts, text lengths, amounts, and
  idempotency keys at the validation boundary.
- Focused rejection tests prove no postings are committed for invalid input.

## Validation

- `./gradlew.bat :app:expense-core:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Registered from HIGH-18 and HIGH-19 in the production-readiness audit. No
implementation claim is made yet.

The locked JPA mutation path now validates payer/allocation identifiers against
active memberships, rejects duplicates within either role, and preserves the
valid case where one member both pays and is allocated a share. Public request
DTOs bound participant counts and category length, and the idempotency header is
bounded through method validation plus explicit controller checks for standalone
and production MVC paths. Expense Core tests pass; focused public HTTP rejection
tests now cover oversized idempotency keys, categories, and payer collections.
