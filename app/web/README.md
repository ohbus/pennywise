# Future UI

Reserved for a later mobile-first web/PWA implementation. The framework,
authentication session strategy, generated GraphQL client, and browser offline
storage are intentionally deferred. The UI will consume `contracts/graphql/` and
must distinguish pending local expense creations from server-confirmed state.
