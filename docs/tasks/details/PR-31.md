# PR-31 — Required production messaging capabilities

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-19, PR-25
- Owned paths: Expense Core and Accounts messaging configuration/tests, and this task detail

## Objective

Prevent deployed services from starting with required event publishing and
passwordless email outbox capabilities disabled.

## Acceptance criteria

- `production` and `staging` require Expense Core outbox and Rabbit publishing.
- `production` and `staging` require Accounts auth-email outbox publishing.
- Local/test profiles retain explicit opt-in defaults for isolated tests.
- Configuration tests prove missing or disabled capabilities fail startup.

## Validation

- `./gradlew.bat :app:expense-core:test :app:accounts:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Added deployed-profile validators requiring Expense Core outbox plus Rabbit
publishing and Accounts auth-email outbox enablement. Local and test defaults
remain disabled unless explicitly enabled. Focused configuration tests cover
missing and valid capability settings.
