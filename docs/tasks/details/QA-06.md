# QA-06 — Isolate JpaOutboxStoreTest from inter-suite database pollution

## Objective

Fix the test failure in `JpaOutboxStoreTest > persists claim retry acknowledgement and parking state()`, which failed at line 44 (`assertTrue(store.claim(10, Duration.ofMinutes(1)).isEmpty())`) when executed as part of the full `:app:expense-core:test` suite.

## Root Cause

`JpaOutboxStoreTest` tests the outbox claim mechanism against the shared in-memory test database without cleaning up between tests. Prior tests in the suite (specifically `JpaGroupStoreTest` during group rename operations) commit `group.renamed.v1` outbox records to `expense_outbox`. When `JpaOutboxStoreTest` subsequently ran, those unconsumed records remained in `expense_outbox` and were returned by `store.claim(10, ...)`, violating the assertion that the outbox was empty.

## Dependencies

- `CORE-10`
- `CORE-22`

## Owned paths

- `app/expense-core/src/test/kotlin/com/subhrodip/pennywise/expensecore/messaging/JpaOutboxStoreTest.kt`
- `docs/tasks/details/QA-06.md`

## Acceptance criteria

- `JpaOutboxStoreTest` purges `expense_outbox` before each test execution via `@BeforeEach`.
- `JpaOutboxStoreTest` and its test methods contain structured KDoc doc comments per `AGENTS.md` and `coding-guidelines.md`.
- `./gradlew :app:expense-core:test --no-daemon` runs cleanly with 100% passing tests (99+ tests, 0 failures).
- Contract validation and git diff checks pass.

## Validation commands

- `./gradlew :app:expense-core:test --no-daemon`
- `python3 tools/contracts/validate.py`
- `git diff --check`

## Evidence

- `./gradlew :app:expense-core:test --no-daemon` completed successfully with 0 failures across the entire test suite.
- Clean working tree and updated task tracker.
