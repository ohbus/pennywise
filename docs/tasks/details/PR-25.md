# PR-25 — Fail closed on production identity-provider wiring

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: AUTH-03, PR-23
- Owned paths: Accounts authentication configuration, focused Accounts context tests, and this task detail

## Objective

Prevent deployed profiles from silently substituting the internal HMAC token
provider when the configured external identity-provider integration is absent.

## Acceptance criteria

- Production, staging, and local-oidc profiles require an explicit
  `IdentityProviderPort` implementation.
- The internal provider remains available only for an explicitly non-deployed
  development profile.
- Accounts tests continue to pass with explicit test fixtures.

## Validation

- `./gradlew.bat :app:accounts:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Registered after the fail-closed configuration review found an unconditional
`@ConditionalOnMissingBean` internal JWT provider. No implementation claim is
made until the profile guard and context tests pass.

The internal provider is now excluded from `production`, `staging`, and
`local-oidc`; those profiles must provide an explicit identity-provider bean.
