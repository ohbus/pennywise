# PR-29 — Checked financial arithmetic

- Phase: production_hardening
- Owner role: coordinator
- Dependencies: PR-17, PR-26
- Owned paths: Expense Core financial arithmetic, focused tests, and this task detail

## Objective

Reject financial requests and derived balances that would overflow signed
64-bit minor-unit arithmetic.

## Acceptance criteria

- Payer, allocation, posting, and settlement aggregation uses checked addition.
- Overflow produces a validation/domain error before ledger side effects.
- Allocation calculations cannot wrap while preserving total amounts.
- Boundary tests cover `Long.MAX_VALUE`, `Long.MIN_VALUE`, and multi-item sums.

## Validation

- `./gradlew.bat :app:expense-core:test --rerun-tasks --no-daemon`
- `git diff --check`

## Current increment

Added checked addition, multiplication, and negation for financial arithmetic;
payer totals, allocation totals/products, and settlement suggestion aggregation
now use the checked helpers. Boundary tests cover aggregation, multiplication,
`Long.MAX_VALUE`, and `Long.MIN_VALUE` overflow.
