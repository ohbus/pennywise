meta {
  name: GraphQL Groups
  type: http
  seq: 2
}

post {
  url: {{baseUrlBff}}/graphql
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "query": "query Groups { groups { id name kind revision members { membershipId subject } } }",
    "operationName": "Groups"
  }
}
